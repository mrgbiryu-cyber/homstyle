# 정규화 항목 및 다음 개발 백로그

기준일: 2026-07-22

## 현재 단계

현재는 엑셀 생성 프로그램을 확장하기 전에, 원천별 값을 어떤 표준 필드와
규칙으로 바꿀지 목록화하는 정규화 설계 단계이다.

상태 표기:

- `적용`: 현재 코드에 반영됨
- `부분`: 일부 상품·표기만 처리됨
- `확정필요`: 카테고리/업무 정의 후 적용
- `다음개발`: 다음 구현 대상으로 기록

## 정규화 항목 목록

| 영역 | 정규화 대상 | 표준화 내용 | 상태 |
|---|---|---|---|
| 상품 식별 | 상품 ID | 홈스타일 `productId` 기준 고유키 | 적용 |
| 원천 식별 | API/HTML/FAQ/Q&A/OCR | 값마다 원천, URL, 수집시각, HTTP 상태 저장 | 적용 |
| 상품 구분 | 홈스타일/가전 | 원천 시스템과 수집기 분리 | 적용 |
| 카테고리 | 대·중·소 카테고리 | 카테고리 경로와 다중 소속 보존 | 적용 |
| 브랜드 | 브랜드명 | 원문 브랜드와 표준 브랜드 분리 후보 | 부분 |
| 이미지 URL | 상대/절대 URL | 절대 URL 변환, 공백·한글 경로 인코딩 | 적용 |
| 대표·롤링 이미지 | 이미지 순서·역할 | 대표 URL, 롤링 순번, 중복 URL 제거 | 적용 |
| 상세 이미지 | 공급사 이미지 목록 | URL 전체 목록과 OCR 후보 순위 | 부분 |
| 옵션 | 옵션 스타일·값 | 색상/사이즈/단일옵션, 옵션 1…N 분리 | 적용 |
| 색상 | 원문 색상 | 원문과 표준 색상·동의어 분리 | 부분 |
| 치수 | W/D/H | 숫자형 mm 컬럼으로 변환 | 적용 |
| 치수 단위 | mm/cm/무단위 | cm→mm, 무단위는 원천·범위별 추론 | 부분 |
| 치수 범위 | 최소~최대 | 원문 보존, 배치 대표값은 최대값 후보 | 적용 |
| 치수 축 순서 | W/D/H 임의 순서 | 축 라벨 기준 재배열 | 적용 |
| 한글 치수축 | 가로/너비/폭·깊이/세로·높이 | W/D/H로 변환 | 적용 |
| 원형 치수 | Dia/Ø/지름+H | 직경을 W와 D에 복제 | 적용 |
| L 축 | L/W/H, W/L/H | 원문 보존, 자동 변환 금지, 의미 확인 후 추가 | 확정필요 |
| 평면형 2축 | 러그·액자·포스터 | 러그 W/D, 액자·포스터 W/H, 나머지 축 해당없음 후보 | 확정필요 |
| 보조 치수축 | SH/SD/AH/SP/T | 외형축과 좌석·돌출·두께 축 분리 | 확정필요 |
| 구성품 치수 | 본체/스툴/프레임/매트리스 | 구성품별 행과 제품 전체 치수 분리 | 부분 |
| 세트상품 | 세트 구성 ID | 구성명과 실제 product ID를 분리 | 확정필요 |
| 공간 | 거실/침실 등 | API 태그와 카테고리 추천 규칙 분리 | 부분 |
| 설치·배치 | 빌트인/스탠딩/벽/천장 | 명시값과 카테고리 추론값 분리 | 부분 |
| 설명서 태그 | 재질/규격/조립/안전 등 | 요청 2 표준 태그 체계로 변환 | 부분 |
| 스타일·분위기 | 미니멀/곡선형/Mood 등 | 원천 명시값과 추론값 분리 | 부분 |
| 의미검색 | 색상·표현 동의어 | 표준어, 동의어, 원문을 함께 저장 | 다음개발 |
| 값 상태 | 확보/부분/미확보/해당없음/추론 | `EXACT/PARTIAL/MISSING/N_A/INFERRED` | 적용 |
| 근거 | 원문·URL·OCR 이미지 | 필드별 evidence와 confidence 저장 | 부분 |
| 필수값 통계 | 별첨 0715의 ●/○ | 적용 필수 그룹 전체 충족=PASS, 하나 이상 결측=보강대상 | 적용 |

## 다음 개발 항목: 상세 이미지 순차 OCR

개발 ID: `ND-IMG-OCR-01`  
우선순위: 높음  
현재 상태: 설계 기록, 미구현

### 목적

현재 대량 OCR은 상세 이미지 URL 전체를 찾더라도 상품별 우선순위 1장만
OCR한다. 다음 개발에서는 상세의 모든 이미지 URL을 후보 목록으로 관리하고,
규격이 확보될 때까지 유효 후보를 순차 OCR한다.

### 처리 규칙

1. `detailInfo`의 상세 이미지 URL을 노출 순서대로 모두 수집한다.
2. 절대 URL 변환, 공백·한글 URL 인코딩, URL/파일 해시 중복 제거를 수행한다.
3. 이미지 역할을 다음과 같이 분류한다.
   - 제품 규격·사이즈·스펙 후보
   - 제품 특징·소재 후보
   - 배송·교환·이벤트·공통 배너
   - 리뷰·연출 이미지
4. `size|spec|dimension|measure|info|규격|치수|사이즈|크기` 신호가 있는
   이미지를 먼저 처리한다.
5. 배송·교환·이벤트·공통 배너는 기본 OCR 대상에서 제외한다.
6. 첫 후보 OCR 후 W/D/H가 미확보 또는 부분확보이면 다음 후보로 진행한다.
7. 상품 외형 W/D/H가 고신뢰로 완성되면 일반 상품은 OCR을 중단한다.
8. 세트상품은 전체값이 발견돼도 구성품별 규격이 완성될 때까지 계속한다.
9. 서로 다른 이미지에서 발견된 축은 같은 제품/구성품 문맥일 때만 병합한다.
10. 채택값마다 이미지 URL, 이미지 순번, OCR 원문, 파싱 규칙, 신뢰도를 저장한다.

### 무조건 모든 이미지를 OCR하지 않는 이유

- 배송·반품·이벤트 문구에서 다른 크기 숫자가 섞여 오탐이 증가한다.
- 동일한 공통 배너가 여러 상품에 반복된다.
- OCR 처리시간과 저장량이 이미지 수에 비례해 증가한다.
- 규격을 이미 확보한 일반 상품에는 추가 OCR 이득이 작다.

따라서 구현 기준은 `전체 이미지 목록화 + 유효 후보 순차 OCR + 충족 시 중단`이다.

### 저장할 추가 필드

- `detail_image_count`
- `detail_image_url_01...N`
- `image_role`
- `image_rank_score`
- `image_hash`
- `download_status`
- `ocr_attempt_order`
- `ocr_status`
- `ocr_dimension_signal`
- `ocr_selected_value`
- `ocr_stop_reason`
- `evidence_image_url`

### 중단 사유 표준값

- `COMPLETE_WDH`
- `COMPLETE_COMPONENT_DIMENSIONS`
- `MAX_CANDIDATES_REACHED`
- `NO_VALID_IMAGE`
- `DOWNLOAD_FAILED`
- `NO_DIMENSION_SIGNAL`
- `MANUAL_REVIEW_REQUIRED`

### 완료 판정 기준

- 상세 이미지 후보 전체 수와 실제 OCR 수행 수를 상품별로 확인할 수 있어야 한다.
- 첫 이미지에서 미확보였던 상품이 추가 이미지로 얼마나 개선됐는지 통계를 낸다.
- 채택된 W/D/H를 원본 이미지 URL과 OCR 문장까지 역추적할 수 있어야 한다.
- 배송·이벤트 이미지 숫자를 제품 외형 치수로 채택하지 않는 회귀 테스트가 있어야 한다.
- 고객용 엑셀에는 채택값만 출력하고 연구용 시트/JSON에는 후보와 탈락 근거를 보존한다.

## 정규화 설계 다음 순서

1. 상세 이미지 순차 OCR
2. L 축의 제조사·카테고리별 의미 정의
3. 러그·액자·포스터의 해당없음 축 정책
4. 침대+매트리스와 소파 세트의 구성품별 규격 모델
5. 세트 구성 실제 product ID 원천 확보
6. 요청 2 태그 사전·동의어·추론값 관리

## 규격 보강 구현 상태 (2026-07-22)

- `ND-IMG-OCR-01`의 부분확보 비평면 상품 154개에 대해 최대 3장 순차 OCR을 1차 실행했다.
- 기존 이미지 중복 제외, URL 재시도, 이미지별 OCR 결과와 중단 사유를 DB에 저장했다.
- 기존 두 축 일치 + 명시축 + 복수 후보 비모호성 기준을 통과한 19개를 반영했다.
- 숫자형 사이즈 옵션 전체가 OCR W축과 대응하고 D/H가 공통인 2개를 추가 반영했다.
- 원본 OCR blob은 `source_ocr_dimension_backup`에 실행명·상품 ID별로 백업한다.
- 검증된 보강값은 원 OCR보다 높은 우선순위의 evidence로 선택한다.
- 전체 상세 이미지 순차 OCR은 아직 다음 개발 항목이다. 현재 실행은 부분확보 비평면 상품과 최대 3장으로 제한했다.
