# 홈스타일 요청 1·2 필드 산출 데이터사전

기준 코드: `build_homestyle_bulk_workbook.py`  
출력: `홈스타일_비음영대상군_전체상품_요구필드_대량결과_4단계판정_검수색상.xlsx`

## 1. 산출 기본 원칙

### 원천 우선순위

```text
상품 API 명시값
→ PDP HTML / FAQ·Q&A
→ 검증된 OCR
→ 카테고리·키워드 추론
→ 미확보 또는 해당없음
```

규격은 별도의 authoritative 원장을 사용한다.

```text
DB 확정
→ 규칙 확정
→ 비교 후보
→ 후보 없음
```

### 상태값

| 값 | 의미 | Excel 색상 |
|---|---|---|
| 실제 값 | 확보 또는 추론 결과 | 요청 1 파랑 / 요청 2 초록 |
| `미확보` | 원천과 규칙에서 값을 만들지 못함 | 분홍 |
| `해당없음` | 상품 유형상 적용하지 않음 | 회색 |
| `확정(DB)` | 기존 원천에서 대표 규격 확정 | 요청 1 |
| `확정(규칙)` | OCR·문맥 규칙으로 대표 규격 확정 | 요청 1 |
| `비교정보 제공` | 자동 확정하지 않고 후보를 쉼표로 제공 | 요청 1 |
| `규격 후보 없음` | 최종 규격 후보 없음 | 분홍 |

## 2. 공통 식별 필드

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 구분 | 실행 대상 시스템 | 홈스타일 배치는 `홈스타일` 고정 |
| 상품 ID | `products.product_id` | 상품 고유키 |
| 상품명 | `goods.data.productName` | HTML 제거·공백 정리, 없으면 상품 ID |

## 3. 요청 1 필드

### 3.1 별첨 0715 필수값 판정

| 출력 필드 | 산출 |
|---|---|
| 별첨0715_필수값 판정 | 규격 원장 기준 `API/HTML 확정`, `OCR·규칙확정`, `비교정보제공`, `최종무후보` |
| 별첨0715_필수값 충족수 | 충족한 필수 그룹 수 |
| 별첨0715_필수값 대상수 | 해당 상품에 적용되는 필수 그룹 수 |
| 별첨0715_보강대상 필드 | 누락 필드명을 `|`로 연결 |

`01_상품별_요구필드`의 `별첨0715_필수값 판정`은 상품별 대표 규격의
최종 처리 상태를 표시한다. 필수 6개 그룹의 충족 여부인 `PASS/보강대상`은
`04_필수값_판정`의 `필수값 판정`과 DB `stg_mandatory_pass.final_status`에
별도로 유지한다.

| 규격 원장 상태 | 별첨0715_필수값 판정 |
|---|---|
| `SOURCE_CONFIRMED` | `API/HTML 확정` |
| `RULE_RESOLVED`, `MANUAL_CONFIRMED` | `OCR·규칙확정` |
| `COMPARISON_PROVIDED`, `REVIEW_REQUIRED` | `비교정보제공` |
| `OCR_REQUIRED`, `NO_CANDIDATE`, `UNCLASSIFIED` | `최종무후보` |

홈스타일 공통 필수 그룹:

1. 브랜드
2. 중·소카테고리
3. 상품 ID
4. 대표 이미지+롤링 이미지 URL
5. 색상
6. 완성된 W/D/H

세트 구성 실제 ID는 2026-07-22 합의에 따라 필수 PASS 판정에서 제외했다.

주의:

- `비교정보 제공` 상품이 3,411개여도 W/D/H 중 하나가 없거나 raw 추론값만
  있으면 필수 W/D/H는 미충족일 수 있다.
- 따라서 최종 무후보 234개와 필수 사이즈 미충족 1,632개는 다른 통계다.

### 3.2 대표·롤링 이미지

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 요청1_대표 이미지 URL | `goods.data.images` | `sortSeq` 오름차순 첫 IMAGE |
| 요청1_롤링 이미지 수 | 동일 | 중복 제거 후 URL 수 |
| 요청1_롤링 이미지 URL 01...N | 동일 | 순서별 개별 열, 현재 최대 18열 |

URL 처리:

- `//...` → `https://...`
- `/goods/...` → `https://static-store.lge.co.kr/goods/...`
- 기타 `/...` → `https://homestyle.lge.co.kr/...`
- 공백·한글 path/query를 URL 인코딩

입력 예:

```json
{
  "sortSeq": "1",
  "type": "IMAGE",
  "imageUrl": "/goods/org/073251020000078073.jpg?aw=1&ah=1&rw=800&rh=800"
}
```

산출:

```text
https://static-store.lge.co.kr/goods/org/073251020000078073.jpg?...
```

### 3.3 규격

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 요청1_대표 규격 대상 | 원장 대표 후보 | `대표 규격`, `비교 대표후보`, 제품/옵션명 |
| 요청1_W (mm) | 규격 원장 | 숫자만 |
| 요청1_D (mm) | 규격 원장 | 숫자만, 2D는 공란/해당없음 정책 |
| 요청1_H (mm) | 규격 원장 | 숫자만 |
| 요청1_규격 상태 | `resolution_status` | 사용자용 한글 상태 |

기본 원천:

- 상품정보고시의 크기/규격/사이즈
- 사이즈 옵션
- PDP HTML의 규격 문장
- FAQ/Q&A 규격 문장
- 상세 이미지 OCR
- context 규칙 원장

단위:

```text
mm → 그대로
cm → ×10
inch → ×25.4
무단위 → 범위·카테고리 기반 추론, 비교정보 전용
```

축:

```text
가로/너비/폭 → W
세로/깊이 → D
높이 → H
L/D/H → W/D/H
W/L/H → W/D/H
L/W/H → W/D/H
지름+높이 → W=지름, D=지름, H=높이
```

### 3.4 규격 비교 후보

| 출력 필드 | 산출 |
|---|---|
| 요청1_규격 비교후보 수 | 후보 행 수 |
| 요청1_규격 비교 W (mm) | 후보별 W를 쉼표 구분 |
| 요청1_규격 비교 D (mm) | 후보별 D를 쉼표 구분 |
| 요청1_규격 비교 H (mm) | 후보별 H를 쉼표 구분 |
| 요청1_규격 비교 대상/옵션 | 같은 순서의 옵션·축·대상 |
| 요청1_규격 비교 원문 | 같은 순서의 원문 |

모든 비교 열은 위치가 맞아야 한다.

예:

```text
후보 수: 2
W: 590, 590
D: 570, 570
H: 450, 810
대상: W,D,H, W,D,H
원문: W=590 D=570 H=450, W=590 D=570 H=810
```

### 3.5 카테고리

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 요청1_중카테고리 | `goods.data.category.categoryName` | 없으면 scope cache |
| 요청1_소카테고리 | `goods.data.category.subCategoryName` | 없으면 scope cache |

대카테고리는 내부 태깅과 요청 2 기본정보에 사용하지만 요청 1의 별도 열은
중·소카테고리만 출력한다.

### 3.6 배치 추천 공간

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 요청1_배치 추천 공간 리스트 | 공간 추천 API+카테고리 | 중복 제거 후 `|` 연결 |

카테고리 fallback 예:

| 카테고리 단어 | 공간 |
|---|---|
| 침대·매트리스·옷장·화장대 | 베드룸 |
| 소파·거실장·거실테이블·러그 | 리빙룸 |
| 식탁·다이닝 | 다이닝룸 |
| 책상·책장·오피스 | 서재 |
| 유아·아동·키즈 | 아이방 |
| 아웃도어·야외 | 테라스 |
| 조명·액자·거울·소품 | 리빙룸 |

### 3.7 브랜드

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 요청1_브랜드명 | `goods.data.brandName` | HTML 제거·공백 정리 |

현재는 브랜드 master code에 매핑하지 않고 원문 브랜드를 사용한다.

### 3.8 색상

우선순위:

1. 상품정보고시 `색상`, `컬러`
2. 옵션 스타일이 색상인 옵션 값
3. 없으면 `미확보`

옵션 값이 상품정보고시 색상 집합에 포함되거나 색상 단어 사전에
일치하면 옵션 스타일을 `색상`으로 정규화한다.

예:

```text
입력: 색상 = 카멜브라운, 크림아이보리
산출: 카멜브라운 | 크림아이보리
```

### 3.9 설치 타입

| 출력 필드 | 홈스타일 산출 |
|---|---|
| 요청1_설치 타입 구분 | `해당없음` |

빌트인/스탠딩 구분은 가전용 조건부 필드다. 현재 대량 결과는 홈스타일만
대상으로 한다.

### 3.10 세트 구성 ID

| 출력 필드 | 입력 | 변환 |
|---|---|---|
| 요청1_세트 구성 ID 리스트 | packages API | 자신을 제외한 product ID 재귀 탐색, `|` 연결 |

상품명에 `세트`, `패키지`, `+`, `2in1`이 있으나 실제 ID를 찾지 못하면
`미확보`; 세트가 아니면 `해당없음`이다.

### 3.11 배치 위치·벽면 높이

| 카테고리 | 요청1_배치 가능 위치 |
|---|---|
| 팬던트·샹들리에·천장·실링 | 천장 |
| 벽등·벽조명·벽걸이·벽거울·벽장식 | 벽 |
| 테이블조명·스탠드조명·액자·소품 | 테이블·선반 위 |
| 그 외 | 바닥 |

`요청1_벽면부착 추천 높이 (mm)`:

- 벽 배치 상품: 명시값이 없으므로 `미확보`
- 벽 배치가 아닌 상품: `해당없음`

### 3.12 옵션

입력:

```text
goods.data.purchaseOptions
goods.data.productStock
```

산출:

| 출력 필드 | 설명 |
|---|---|
| 요청1_옵션 스타일 | 색상/사이즈/단일옵션/원문 스타일 |
| 요청1_옵션 수 | 전체 옵션 값 수 |
| 요청1_옵션 01...N | 옵션 값, 현재 최대 77열 |

여러 옵션 스타일이 있으면 값 앞에 스타일을 붙인다.

```text
색상: 네이비
사이즈: 1600
```

## 4. 요청 2 필드

요청 2는 명시값과 규칙 기반 태그가 혼합되어 있다. 현재 LLM 모델은
사용하지 않는다.

### 4.1 가구 상품 설명서 자동 태깅

| 출력 필드 | 주 원천 | 산출 |
|---|---|---|
| 요청2_설명서태그_기본 | 상품 API | 상품명, 브랜드, 대>중>소 분류 |
| 요청2_설명서태그_제조 | 상품정보고시 | 제조자/제조사/수입자+제조국/원산지 |
| 요청2_설명서태그_재질 | 상품정보고시, Q&A | 소재/재질, 없으면 공개 Q&A 탐색 |
| 요청2_설명서태그_규격 | 기본 파서 규격 목록 | `W=..., D=..., H=... mm` |
| 요청2_설명서태그_구성품 | 상품정보고시 | 구성품/제품구성 |
| 요청2_설명서태그_색상 | 요청 1 색상 | 동일 값 |
| 요청2_설명서태그_사용목적 | 카테고리 공간 | `공간 + 제품 용도` 문장 |
| 요청2_설명서태그_조립 | 상품정보고시 | 조립/설치, 일부 상품은 해당없음 |
| 요청2_설명서태그_안전 | 상품정보고시 | 안전/주의 |
| 요청2_설명서태그_취급주의 | 상품정보고시, serviceGuide | 세탁/취급/관리/주의 |
| 요청2_설명서태그_품질보증 | 상품정보고시 | 품질보증/보증 |
| 요청2_설명서태그_인증 | 상품정보고시 | 인증/허가/KC |
| 요청2_설명서태그_판매정보 | 상품 API | 판매상태, 판매가 범위, 수집시점 |

### 4.2 디자인 스타일 추론

입력:

- 상품명
- 상세 설명 앞부분
- 공간 추천 API의 hashtag/title/category

키워드 매핑 예:

| 입력 단어 | 표준 태그 |
|---|---|
| 미니멀 | 미니멀 |
| 모던 | 모던 |
| 컨템포러리 | 컨템포러리 |
| 북유럽 | 스칸디나비안 |
| 빈티지·레트로 | 빈티지/레트로 |
| 내추럴·우드 | 내추럴 |
| 곡선·라운드·아치 | 곡선형 |
| 모듈 | 모듈형 |
| 앤틱 | 클래식 |

태그가 하나도 없으면:

```text
컨템포러리 (카테고리 기반 추론)
```

따라서 이 필드는 명시 디자인 taxonomy가 아니라 규칙 기반 추론이다.

### 4.3 공간 콘텐츠 자동태깅

| 출력 필드 | 원천/산출 |
|---|---|
| 요청2_공간태그_인테리어스타일 | space API 태그, 없으면 제품 스타일 |
| 요청2_공간태그_분위기 | collection title |
| 요청2_공간태그_공간명 | space API+카테고리 fallback |
| 요청2_공간태그_공간목적 | 공간+카테고리 용도 문장 |
| 요청2_공간태그_색상톤 | 제품 색상 기반 |
| 요청2_공간태그_공간크기 | 현재 명시 원천 없어 `미확보` |
| 요청2_공간태그_포함제품목록 | collection.products의 productId |
| 요청2_공간태그_배치사유·위치 | collection subtitle, 없으면 배치 위치 |

### 4.4 관계정보

| 출력 필드 | 산출 |
|---|---|
| 요청2_공간스타일↔제품 관계정보 | 공간 스타일+현재 상품 연결 |
| 요청2_제품간 관계정보 | 동일 collection의 productId를 `↔`로 연결 |

현재는 관계 DB의 edge table이 아니라 Excel용 문자열이다.
운영 추천 시스템에서는 다음과 같은 별도 테이블로 분리하는 것이 좋다.

```text
space_id
source_product_id
target_product_id
relation_type
evidence_collection_id
score
```

### 4.5 개인별 CRM

| 출력 필드 | 산출 |
|---|---|
| 요청2_개인별 구매·선호·보유·CRM | `해당없음` 또는 `미확보` |

현재 수집 범위에는 개인 데이터가 없으며, 개인정보는 수집하지 않는다.

### 4.6 의미기반 검색·추천

현재는 임베딩/LLM 검색이 아니라 검색용 설명 문자열과 색상 동의어를 만든다.

예:

```text
까사미아 벨로씨 소파 일반소파 색상=카멜브라운
동의어=브라운|Brown
```

색상 동의어 예:

```text
레드 ↔ 빨강|적색|붉은색
블루 ↔ 파랑|청색|푸른색
그레이 ↔ 회색|Gray|Grey
베이지 ↔ 아이보리|크림
```

운영 의미검색을 위해서는 이 문자열을 embedding index에 적재하는 별도
개발이 필요하다.

## 5. 대표 입력·산출 예

### 5.1 상품 API만으로 요청 1 산출

상품 `G25070005743`

입력:

```text
brandName = 까사미아
category = 가구 > 소파 > 일반소파
색상 = 카멜브라운, 크림아이보리
크기 = [4인] W2910 X D1020 X H910mm /
       [스툴] W740 X D660 X H410mm
옵션 = 카멜브라운, 크림아이보리
```

산출:

```text
브랜드명 = 까사미아
중카테고리 = 소파
소카테고리 = 일반소파
제품 색상 = 카멜브라운 | 크림아이보리
대표 W/D/H = 2910 / 1020 / 910
배치 추천 공간 = 리빙룸
배치 위치 = 바닥
옵션 스타일 = 색상
규격 상태 = 확정(DB)
```

### 5.2 OCR을 추가해 확정

상품 `G25090019410`

입력:

```text
상품정보고시 크기 = 상세페이지 참조
OCR = INFO ... 가로기600 * 세로2140 * 헤드높이800mm
```

산출:

```text
가로기600 → 가로2600
W/D/H = 2600 / 2140 / 800
규격 상태 = 확정(규칙)
```

### 5.3 의미를 결정하지 못하면 비교 후보

상품 `G25110025989`

입력:

```text
가로 590mm
깊이 570mm
높이 810mm
좌면 높이 450mm
```

산출:

```text
후보 1 = 590 / 570 / 450
후보 2 = 590 / 570 / 810
규격 상태 = 비교정보 제공
```

### 5.4 입력이 불충분하면 후보 없음

상품 `G25070000483`

입력:

```text
옵션 = Ø100, Ø120
높이 없음
상세 이미지 OCR 규격 신호 없음
```

산출:

```text
W/D/H = 공란
규격 상태 = 규격 후보 없음
잔여 원인 = 전체 이미지에서 크기 신호 없음
```

## 6. Excel 시트

| 시트 | 내용 |
|---|---|
| `00_요약` | 대상 수, 필수 PASS, 규격 상태, 원천별 성공, 잔여 원인 |
| `01_상품별_요구필드` | 요청 1·2 전체 평면 결과 |
| `02_규격_상세` | API/HTML/OCR 기본 규격 레코드 |
| `03_옵션_상세` | 옵션 스타일·옵션값·옵션 ID·이미지 |
| `04_필수값_판정` | 상품별 필수 그룹 충족 여부 |
| `05_카테고리_통계` | scope·상품·필수값 통계 |
| `06_수집오류` | 원천별 HTTP·수집 상태 |
| `07_조합상품_구성품` | 동일 상품 ID를 반복하고 `component_seq`로 나눈 구성품 규격 |

`01_상품별_요구필드`는 현재 157열이다.

- 공통 식별 3열
- 요청 1·2 데이터 154열
- 요청 1 조합상품 관련 5열:
  `조합상품 여부`, `판정근거`, `구성품 수`, `규격 확정 수`, `분리 상태`
- 롤링 이미지 18열과 옵션 77열은 현재 데이터의 최대 개수에 따라 동적이다.

`07_조합상품_구성품`은 현재 2,111행, 17열이다. 기본 키는
`(상품 ID, 구성품 순번)`이며 W/D/H는 mm 숫자형 셀이다.

## 7. DB와 Excel의 차이

현재 DB에는 다음이 저장된다.

- 원천 API/HTML/Q&A/OCR BLOB
- 규격 후보·규격 원장·비교 후보
- 조합상품 판정 및 구성품별 규격 snapshot
- 필수값 판정 snapshot
- 잔여 원인과 상태 이력

조합상품 관련 DB 객체는 다음과 같다.

- `stg_product_combination`
- `stg_product_component_dimension`
- `vw_product_combination_current`
- `vw_product_component_dimensions_current`
- `vw_product_combination_summary`

그러나 요청 1·2의 154개 Excel 필드를 한 행으로 저장한 최종 평면 fact
테이블은 없다. 일부 요청 2 태그와 공간 문자열은 Excel 생성 시점에
계산된다.

운영 API나 검색 시스템에 사용하려면 다음 테이블이 추가로 필요하다.

```text
fact_product_requested_field
  snapshot_id
  product_id
  field_code
  value_text
  value_number
  value_status
  source_type
  source_ref
  confidence
  calculated_at
```

또는 상품별 wide table과 필드별 evidence table을 분리할 수 있다.

## 8. 현재 통계 해석

| 통계 | 수치 | 의미 |
|---|---:|---|
| 전체 상품 | 9,358 | 홈스타일 비음영 대상 |
| 확정 규격 | 5,713 | 대표 W/D/H 잠금 |
| 비교정보 제공 | 3,411 | 후보를 제공하지만 자동 확정하지 않음 |
| 최종 무후보 | 234 | 추가 원천 또는 OCR 개발 필요 |
| 규격 분석 가능 | 9,124 | 확정+비교, 97.5% |
| 필수값 PASS | 7,372 | 6개 필수 그룹 모두 충족, 78.8% |
| 필수 사이즈 미충족 | 1,632 | 완성 W/D/H가 없는 상품 |
| 색상 미충족 | 737 | 색상 원천·옵션 없음 |

비교정보 제공은 “정보가 있다”는 뜻이며 “제품 대표 W/D/H가 확정됐다”는
뜻이 아니다. 개발 시 두 상태를 합치면 안 된다.
