# 2차 OCR DB 적재 결과 (2026-07-23)

## 1. 실행 범위

- 실행명: `dimension_scan_pass2_layout_v1`
- 1차 OCR에서 2차 대상으로 분류된 상품: 2,308개
- 상품-이미지 참조: 4,292건
- 중복 제거 이미지: 1,856개
- 좌표 기반 OCR 성공: 1,810개
- 이미지 준비 실패: 46개
- 생성된 규격 관측값: 6,949건

46개 준비 실패의 호스트는 `maisonelbara.com` 44개, `thedotdesign.cafe24.com` 1개, `heywds.cafe24.com` 1개이다. 현재 URL 서버 연결 실패로 원본 이미지를 다시 받지 못한 건이며 재수집 대상으로 유지했다.

## 2. 상품별 최종 분류

| 분류 | 상품 수 | 의미 |
|---|---:|---|
| `COMPLETE_EXPLICIT_WDH_CANDIDATE` | 736 | W/D/H와 단위를 명시적으로 읽음. 대표 규격·옵션 충돌 검증 후 적용 후보 |
| `COMPLETE_WDH_UNIT_MISSING` | 24 | W/D/H 세 축은 있으나 단위 검증 필요 |
| `COMPLETE_NONSTANDARD_AXES_REVIEW` | 298 | L/W/H 등 비표준 축 조합. W/D/H로 자동 매핑하지 않음 |
| `TRIPLET_MAPPING_REVIEW` | 198 | 숫자 3개는 있으나 축 순서 판단 필요 |
| `PARTIAL_AXES_REVIEW` | 623 | 축이 일부만 확보됨 |
| `NUMERIC_CLUSTER_REVIEW` | 369 | 규격 주변 숫자 군집은 있으나 제품/옵션/구성품 분리 필요 |
| `NO_DIMENSION_OBSERVATION` | 14 | OCR은 성공했으나 규격 관측값이 없음 |
| `OCR_INCOMPLETE_NO_CANDIDATE` | 46 | 이미지 재수집 오류로 2차 OCR 관측값이 없음 |
| **합계** | **2,308** |  |

- 규격 관련 관측값이 하나 이상 생긴 상품: 2,248개
- 2차 OCR 대상 중 신규 관측값이 없는 상품: 60개
- 기존 1차 무정보군 2,455개 전체 기준으로 2차 신규 관측값이 없는 잔여: 207개
  - 1차에서 2차 대상 이미지 자체가 없던 147개
  - 2차 대상 중 무관측·재수집 오류 60개

`COMPLETE_EXPLICIT_WDH_CANDIDATE` 736개도 원본 상품 규격 컬럼에 확정 반영하지 않았다. 한 상세 이미지에 여러 옵션·구성품 규격이 있을 수 있으므로 현재 단계에서는 자동 추출 후보이다.

## 3. DB 적재 위치

- DB: `homestyle_bulk_run/homestyle_bulk.sqlite`
- 실행 요약: `homestyle_bulk_run/dimension_scan_pass2_layout_v1.json`
- 원본 좌표 OCR 결과: `homestyle_bulk_run/ocr/dimension_scan_pass2_layout_v1/layout_ocr_00.json` ~ `layout_ocr_07.json`

### 테이블

| 테이블 | 행 수 | 용도 |
|---|---:|---|
| `stg_dimension_scan_pass2_unique_image` | 1,856 | 이미지별 OCR 상태·전체 OCR 텍스트·오류 |
| `stg_dimension_scan_pass2_observation` | 6,949 | 이미지에서 추출한 모든 규격 관측값·좌표·근거 |
| `stg_dimension_scan_pass2_product_image` | 4,292 | 상품과 후보 이미지·최상 관측값 연결 |
| `stg_dimension_scan_pass2_product` | 2,308 | 상품별 대표 후보·상태·다음 작업 |

### 조회 뷰

- `vw_dimension_scan_pass2_complete_wdh`: 명시적 W/D/H+단위 후보만 조회
- `vw_dimension_scan_pass2_review_queue`: 사람 검토가 필요한 상품 큐
- `vw_dimension_scan_pass2_status_summary`: 실행·상태별 상품 수
- `vw_dimension_scan_pass2_best_evidence`: 상품별 최상 후보와 OCR 근거·좌표 연결
- `vw_dimension_scan_pass2_prepare_errors`: 이미지 준비/OCR 실패 이미지
- `vw_dimension_pattern_with_pass2_current`: 기존 규격 패턴 작업 큐와 2차 결과 결합

## 4. 오탐 방지 규칙과 확인 사례

- 숫자 뒤 영문 축은 `1,862(W)`처럼 괄호로 묶인 경우만 축으로 인정한다.
- `8311 white`처럼 색상 코드 뒤의 단어 첫 글자를 W축으로 오인하지 않는다.
- 명시적 W/D/H라도 20mm 미만 또는 5,000mm 초과 값은 범위 검토 상태로 강등한다.
- L/W/H는 카테고리와 도면 해석 전까지 W/D/H에 자동 매핑하지 않는다.

검증 상품 `G25070002012`는 최종적으로 `W=900mm, D=470mm, H=350mm`가 적재됐고, 기존 오탐 후보 `W=8311`은 관측 테이블에서 제거됐다.

## 5. 반영 경계

- 이번 실행은 스테이징 테이블과 조회 뷰까지만 갱신했다.
- 기존 상품 원천 규격 값은 변경하지 않았다.
- Excel 파일은 변경하지 않았다.
- 다음 단계는 736개 직접 후보의 대표 규격/옵션 충돌 검증과 46개 오류 이미지 재수집이다.
