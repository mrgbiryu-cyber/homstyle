# 규격 진행상태 단일 원장

작성일: 2026-07-23  
원장 버전: `dimension-resolution-ledger-v2-comparison`

## 왜 207건만 남은 것처럼 보였는가

기존의 `classification_complete=1`은 규격값이 확정됐다는 뜻이 아니었다.

당시 9,151건은 다음 중 하나의 **작업 분류가 배정된 상품**이었다.

- 기존 W/D/H 확정
- 완전한 규격 후보 존재
- 일부 축만 존재
- OCR 이미지 후보 존재
- 규격 신호 없음

따라서 `9,151건 분류 완료 / 207건 미분류`는 **작업 그룹 분류율**이다.  
제품별 대표 W/D/H의 확정률이 아니다.

앞으로 진행 통계에는 이 수치를 사용하지 않고, 아래 단일 원장의 잠금 상태만 사용한다.

## 현재 권위 있는 진행 통계

| 원장 상태 | 상품 수 | 잠금 | 다음 처리 |
|---|---:|---|---|
| SOURCE_CONFIRMED | 4,152 | O | 제외 |
| RULE_RESOLVED | 1,017 | O | 제외 |
| COMPARISON_PROVIDED | 1,353 | X | 대표값과 모든 후보를 엑셀에 제공; 확인 대기 제외 |
| REVIEW_REQUIRED | 0 | X | 없음 |
| OCR_REQUIRED | 636 | X | 규격 구역 표적 재OCR |
| NO_CANDIDATE | 1,993 | X | 전체 이미지 스캔 후 OCR |
| UNCLASSIFIED | 207 | X | 이미지·소스 탐색부터 수행 |
| 합계 | 9,358 |  |  |

- 실제 누적 확정 및 잠금: **5,169건**
- 비교정보 제공으로 처리한 상품: **1,353건**
- 사람 확인 대기: **0건**
- OCR 파이프라인 대상: **2,836건**
- 전체 미완료 작업 큐: **2,836건**

`RULE_RESOLVED`는 원본 DB 확정값과 구분된다. 검증된 규칙으로 자동 결정한 값이며, 현재 원장에서는 재처리 방지를 위해 잠근다.

`COMPARISON_PROVIDED`는 대표값을 확정했다는 의미가 아니다. 기존 대표
후보는 W/D/H 숫자 컬럼에 제공하고, 모든 유효 후보는 바로 옆 비교
W/D/H·대상·원문 컬럼에 같은 순서로 쉼표 구분해 제공했다는 의미다.
이 정책으로 별도 사람 확인 작업은 진행하지 않는다.

## OCR을 다시 돌려야 하는 범위

전체 9,358건이나 미확정 4,999건을 다시 OCR할 필요는 없다.

1. `SOURCE_CONFIRMED`, `RULE_RESOLVED` 5,169건
   - 다시 OCR하지 않는다.
   - 규칙을 명시적으로 폐기하지 않는 한 이후 스냅샷이 값을 낮추거나 제거하지 못한다.

2. `COMPARISON_PROVIDED` 1,353건
   - 이미 완전한 규격 후보가 있다.
   - 저장된 OCR을 다시 실행하지 않는다.
   - 대표 후보와 비교 후보 4,652개를 엑셀에 제공했다.
   - 복수 후보 상품 917건은 W/D/H·대상·원문을 위치가 맞도록 쉼표 구분했다.
   - 사람 확인 및 OCR 작업 큐에서는 제외한다.

3. `OCR_REQUIRED` 636건
   - 일부 축 또는 OCR 결합 오류가 있다.
   - SIZE/DIMENSION/INFO 구역만 표적 재OCR한다.

4. `NO_CANDIDATE` 1,993건
   - 현재 저장 OCR에서 완전 규격 후보가 없다.
   - 전체 상세 이미지를 1차 스캔하고, 발견된 규격 구역을 2차 OCR한다.

5. `UNCLASSIFIED` 207건
   - API/HTML 이미지 목록과 상세 이미지 존재 여부부터 다시 확인한다.

## 누적 처리 원칙

1. 원장의 `is_locked=1` 상품을 모든 후속 작업 대상에서 제외한다.
2. 새 OCR 결과에 확정 규칙이 적용되면 `RULE_RESOLVED`로 승격하고 잠근다.
3. 사람이 후보를 승인하면 `MANUAL_CONFIRMED`로 승격하고 잠근다.
4. 복수·문맥 후보는 `COMPARISON_PROVIDED`로 누적하고 모든 후보를 엑셀에 제공한다.
5. 값이 없는 상품만 `OCR_REQUIRED`, `NO_CANDIDATE`, `UNCLASSIFIED`에 남긴다.
6. 모든 상태 변화는 이력 테이블에 누적한다.

원장 생성 직후 같은 작업을 두 번 실행해 검증한 결과:

- 첫 실행 상태 전환: 9,358건
- 두 번째 실행 상태 전환: 0건
- 비교정보 제공 정책 전환: 1,353건
- 정책 적용 후 재실행 상태 전환: 0건
- 중복 이력 추가: 0건
- DB 무결성 검사: `ok`

## DB 테이블과 조회 뷰

DB: `homestyle_bulk_run/homestyle_bulk.sqlite`

- `fact_dimension_resolution_ledger`
  - 9,358건 전체의 현재 권위 상태
  - 잠금값과 검토 후보값을 별도 컬럼으로 관리
- `fact_dimension_resolution_option`
  - 잠금된 대표/복수 규격 5,191행
- `hist_dimension_resolution_event`
  - 상태·값 전환 이력
- `ref_dimension_resolution_rule`
  - 확정된 정규화·선택·제외·OCR 보정 규칙
- `vw_dimension_progress_authoritative`
  - 앞으로 사용할 단일 진행 통계
- `vw_dimension_locked_current`
  - 후속 작업에서 제외할 상품
- `vw_dimension_remaining_human_review_current`
  - 사람 확인 대상 0건
- `vw_dimension_comparison_provided_current`
  - 비교정보 제공으로 처리한 상품 1,353건
- `vw_dimension_comparison_candidates_current`
  - 엑셀에 제공하는 중복 제거 규격 후보 4,652건
- `vw_dimension_remaining_ocr_current`
  - OCR 파이프라인 대상 2,836건
- `vw_dimension_work_queue_current`
  - 전체 미완료 2,836건과 작업 유형

## 실행 파일

`build_dimension_resolution_ledger.py`

- 기존 확정값과 규칙 확정값을 원장에 누적
- 잠긴 값의 자동 하향 변경 방지
- 대표/복수 옵션 잠금 저장
- 미확정 후보 비교 제공 정책 누적
- 상태 변화 이력 기록
- 작업 큐 및 단일 통계 갱신

## 엑셀 출력

`홈스타일_비음영대상군_전체상품_요구필드_대량결과_규격비교.xlsx`

`01_상품별_요구필드`에서 기존 W/D/H 바로 다음에 아래 열을 배치했다.

- `요청1_규격 비교후보 수`
- `요청1_규격 비교 W (mm)`
- `요청1_규격 비교 D (mm)`
- `요청1_규격 비교 H (mm)`
- `요청1_규격 비교 대상/옵션`
- `요청1_규격 비교 원문`

후보가 여러 개인 경우 모든 비교 열은 동일한 후보 순서를 유지하며 쉼표로
구분한다. 비교 열의 헤더 색상은 요청 1 색상과 동일하다.
