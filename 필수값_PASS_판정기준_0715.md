# 3D 애셋 필수값 PASS 판정 기준

원천: `(발췌) 별첨. 3D 애셋 생성운용 자동화 필요데이터 _0715_ing_R.pptx`  
기준일: 2026-07-22

## 원문 기호 해석

- `●`: 필수
- `○`: 선택

## 필수 항목

### 모든 상품 공통

| 필수 그룹 | 프로그램 판정 기준 | 미확보 시 보강대상 |
|---|---|---|
| 브랜드 | 브랜드명이 `미확보/해당없음/빈값`이 아님 | 브랜드 |
| 카테고리 | 중카테고리와 소카테고리가 모두 존재 | 카테고리(중·소) |
| ID | 홈스타일 product ID 또는 가전 model ID 존재 | ID |
| PDP 이미지 | 대표 이미지 URL과 롤링 이미지 URL이 모두 존재 | 대표·롤링 이미지 URL |
| Color | 제품 색상 존재 | 색상 |
| Size | W, D, H가 모두 숫자이고 mm로 정규화됨 | 사이즈(W/D/H) |

공통 필수 그룹은 6개이다. 카테고리와 사이즈는 그룹 내부의 모든 하위값이
채워져야 해당 그룹을 충족한 것으로 본다.

### 조건부 필수

| 적용 조건 | 필수 그룹 | 프로그램 판정 기준 |
|---|---|---|
| 가전 | 가전 후면 이미지 | 후면 이미지 URL 존재 |
| 가전 | 가전 추가사항 | 설치·배치 유형의 명시값 존재 |
| 세트상품 | 세트 구성 실제 ID | 참고 필드로 유지하며 PASS 필수 판정에서는 제외(2026-07-22 정책 변경) |

가전·세트 조건이 아닌 상품에는 조건부 필드를 `해당없음`으로 두고 PASS
분모에서 제외한다.

## 선택 항목

다음 항목은 계속 수집하지만 PASS/보강대상 판정에는 사용하지 않는다.

- 홈스타일 후면·좌측·우측 이미지
- Texture: 옵션 레이블과 적용 제품 이미지
- 제품 배치 가능 위치
- 벽면 부착 추천 높이
- 추천 공간

## 상품 단위 최종 판정

```text
PASS = 적용되는 필수 그룹이 모두 충족
보강대상 = 적용되는 필수 그룹 중 하나 이상 미충족
```

- 두 필수 그룹이 비어 있어도 상품 수는 보강대상 1개로 집계한다.
- 보강 사유별 통계에서는 비어 있는 필드 각각을 중복 집계한다.
- 상품 수집 자체가 실패한 상품은 필수값을 판정할 수 없으므로 보강대상으로
  포함하고 `상품 수집오류`로 별도 집계한다.

## 이미지 필수값 대체 기준

대표 이미지 URL과 롤링 이미지 URL이 모두 입력되어 있으면 별첨의 필수 이미지
요건을 충족한 것으로 대체 판정한다.

- 롤링 이미지가 한 장 이상 있으면 충족한다.
- 롤링 이미지 01이 대표 이미지와 같은 URL이어도 허용한다.
- 정면/45도, 해상도, 화이트·누끼 배경은 PASS 필수 판정에서 제외한다.
- 이미지 상태는 `URL 대체충족`으로 기록한다.

- 상세 이미지 순차 OCR: `ND-IMG-OCR-01`

## 엑셀 출력 변경사항

다음 대량 엑셀 재생성부터 아래 정보를 출력한다.

- `00_요약`: 필수값 PASS/보강대상/PASS율/보강 사유별 상품 수
- `01_상품별_요구필드`: 규격 처리 4단계 판정, 충족수, 대상수, 보강대상 필드
- `04_필수값_판정`: 필수 6개 그룹의 `PASS/보강대상` 판정
- `04_필수값_판정`: 상품별 필수 그룹 충족 여부와 이미지 필수 대체판정

기존 추천 공간과 요청 2 전체 셀 확보율은 참고 통계로 유지하되, 최종 상품
PASS 통계는 이 별첨 필수값 기준을 우선한다.

### 2026-07-22 필수값 정책 변경

`세트 구성 실제 ID`는 고객 제공 필드와 데이터 품질 확인 항목으로는 유지하지만, PASS/보강대상 판정의 필수 그룹에서는 제외한다. 값이 비어 있어도 해당 사유만으로 보강대상이 되지 않으며, 충족수·대상수·보강대상 필드 통계에도 포함하지 않는다.

## 현재 홈스타일 임시 기준선

엑셀을 저장하지 않고 현재 DB 9,358개를 새 기준으로 계산한 결과이다.

| 판정 | 상품 수 | 비율 |
|---|---:|---:|
| PASS | 3,955 | 42.3% |
| 보강대상 | 5,403 | 57.7% |

보강 사유는 한 상품에 여러 개가 중복될 수 있다.

| 보강 필수 그룹 | 상품 수 |
|---|---:|
| 사이즈 W/D/H | 5,208 |
| 색상 | 737 |

세트 구성 실제 ID 미확보는 1,152개이지만 참고 통계이며 위 필수 보강 사유에는 포함하지 않는다.

브랜드, 중·소카테고리, 상품 ID, 대표 이미지 URL과 롤링 이미지 URL은 현재
데이터 기준으로 9,358개 모두 충족했다. 이미지 필수값은 합의한 URL 대체
기준으로 계산했다.
