# 홈스타일 규격 파싱 규칙 기록

기준일: 2026-07-22

이 문서는 `build_homestyle_bulk_workbook.py`의 규격 파서를 다시 보완하거나
엑셀을 재생성할 때 적용할 규칙과 보류 규칙을 기록한다.

## 적용 완료한 정규식 규칙

다음 규칙은 파서 코드에 반영됐지만, 기존 고객용 엑셀은 아직 재생성하지 않았다.

1. W/D/H 임의 순서
   - 예: `W87 x H74(SH32) x D87 cm`
   - 동일 원천의 180자 이내에서 W, D, H가 모두 확인될 때만 채택한다.
   - SH, SD, AH는 좌석·팔걸이 보조축이므로 외형축으로 사용하지 않는다.

2. 한글 3축
   - W: `가로|너비|폭`
   - D: `깊이|세로`
   - H: `높이`
   - 예: `너비 49.6 x 깊이 52 x 높이 74.5 cm`

3. 직경과 높이
   - 직경: `DIA|Dia.|Ø|Φ|⌀|지름|직경`
   - 직경과 H가 같은 짧은 규격 표현 안에 있으면 W와 D에 직경을 각각 넣는다.
   - 예: `Ø35 x H12cm` → W=350, D=350, H=120mm

4. 범위값
   - 예: `높이 710-900mm`
   - 배치 바운딩박스용 대표값은 최대값 900을 사용한다.
   - 원문은 규격 상세의 근거로 그대로 보존한다.

## L 축 처리 — 해석 후 추가

`L`은 현재 W/D/H로 자동 변환하지 않는다.

- `W98 x L48 x H53`과 `L190 x W80 x H75`는 같은 고정 규칙으로 변환하면
  방향이 뒤집힐 수 있다.
- 현재 파서는 L이 명시된 표현을 정규화 W/D/H 완전값으로 채택하지 않는다.
- 원문의 L/W/H 값과 표기 순서는 근거 데이터로 보존한다.
- 제조사 표기 관례, 카테고리 좌표계 또는 3D Asset 축 정의를 확인한 뒤
  별도의 매핑 규칙으로 추가한다.

권장 추가 컬럼:

- 원본 L/W/D/H
- 정규화 W/D/H
- 축 매핑 상태: `원천확정`, `카테고리추론`, `해석대기`
- 축 매핑 근거

## 카테고리별 적용 기준 후보

아직 고객 분류 기준이 확정되지 않아 엑셀에는 적용하지 않았다.

- 일반 3D 가구: W/D/H 필요
- 러그: W/D 필요, H는 해당없음 후보
- 액자·인테리어포스터: W/H 필요, D는 해당없음 후보
- 원형 테이블·조명: 직경을 W/D로 변환
- 세트상품: 구성품별 규격을 별도 행으로 관리
- 가변형 제품: 축별 최소/최대값을 별도 보존
- 벽조명: SP/T는 돌출 깊이 후보이나 카테고리 조건이 필요
- 의자·소파: SH/SD/AH는 외형 H/D와 분리

### 평면형 상품의 2축 정규식 후보

미확보 원문에서 다음 OCR/옵션 표기가 반복 확인됐다.

- 일반 표기: `100x150`, `70X100`
- OCR 구분자 오인식: `60)(60cm`, `70Ⅹ70cm`
- 후보 정규식:
  `(?<![\d.])(\d{1,4}(?:[.,]\d+)?)\s*(?:mm|cm)?\s*(?:x|×|Ⅹ|\*|\)\s*\()\s*(\d{1,4}(?:[.,]\d+)?)\s*(?:mm|cm)?(?![\d.])`
- 단위가 없으면 상품 옵션·상품정보고시·상품명처럼 구조화된 원천에 한해
  cm 후보로 해석한다.
- OCR/HTML은 앞뒤 90자 안에 `Size|규격|치수|크기|사이즈|실측`이 있을 때만
  고신뢰 후보로 채택한다.
- 러그는 첫 값→W, 둘째 값→D, H→해당없음 후보이다.
- 액자·인테리어포스터는 첫 값→W, 둘째 값→H, D→해당없음 후보이다.
- 이 규칙으로 현재 잔여 중 227개(러그 91, 액자 96, 포스터 40)가 추가
  확보 후보로 확인됐다. 카테고리 축 정의가 확정된 뒤 적용한다.

## OCR 이미지 URL 정규화

공급사 이미지 URL의 공백·한글 경로 때문에 OCR 다운로드가 실패한 사례가
확인되어 `bulk_homestyle_ocr.py`에 URL 인코딩을 반영했다.

- 이미 인코딩된 `%`는 보존한다.
- path/query의 공백과 비ASCII 문자를 UTF-8 percent encoding한다.
- 잔여 다운로드 실패 442개 중 423개가 이 유형이었다.

## 2026-07-22 분석 수치

- 대상 상품: 9,358개
- 기존 고객용 엑셀의 완전 확보 집계: 3,563개
- 기존 고객용 엑셀 주 시트에서 W/D/H가 모두 노출된 상품: 3,531개
  - 32개는 뒤쪽 원천에 완전값이 있었으나 첫 규격 레코드를 대표값으로
    선택한 문제였고, 대표값 선택 로직을 보완했다.
- 확정 정규식 적용 및 L 자동변환 제외 후 완전 확보: 4,129개
- 러그·액자·포스터의 기존 2축까지 포함한 유효 충족: 4,607개
- 잔여: 4,751개
- L 해석 대기: 127개
  - 축 포함 집합 기준 `L/W/H` 123개, `L/D/H` 4개
  - 원문 축 순서 타입으로는 `W-L-H` 113개, `L-W-H` 10개,
    `L-D-H` 4개로 분리
  - 기존 주 시트에서 완전 확보로 보였던 119개가 안전하게 보류 상태로
    재분류됐다.
- 확정 정규식으로 기존 주 시트 대비 새로 완전 확보된 상품: 717개

## 미확보 OCR 표본 연구

고객용 DB/엑셀에 병합하지 않은 200개 연구 표본 결과이다.

- URL 인코딩 실패 재시도: 60개 중 58개 다운로드 성공, 31개 W/D/H 완성
- L 보류로 OCR 미실행: 40개 중 새 규격 신호 27개, W/D/H 완성 0개
- 첫 이미지에 규격이 없던 상품의 대체 이미지: 100개 중 새 규격 신호
  32개, W/D/H 완성 5개

따라서 우선순위는 URL 인코딩 실패 전체 재처리, 카테고리별 2축 규칙 확정,
세트 구성품별 규격 분리 순이다. L은 OCR 추가가 아니라 축 의미 확인이 먼저다.

분석 원본: `homestyle_bulk_run/dimension_notation_analysis.json`

잔여 원인: `homestyle_bulk_run/remaining_dimension_research.json`

OCR 표본: `homestyle_bulk_run/research_missing_ocr_sample.json`

## 2026-07-22 부분확보 규격 보강 2차

부분확보 중 평면 카테고리를 제외한 154개 상품을 대상으로, 기존 OCR 이미지와 중복되지 않는 상세 이미지를 상품당 최대 3장씩 순차 처리했다.

- 후보 이미지: 427장
- 다운로드 성공: 416장
- 자동 검증 후 반영: 19개 상품
- 옵션 값과 OCR 규격을 1:1 결합해 반영: 2개 상품
- 최종 W/D/H 완전확보: 4,129 → 4,150개(+21)
- 규격 보강 직후 필수 PASS: 3,263 → 3,279개(+16)
- 세트 구성 실제 ID 필수 제외 후 PASS: 3,955개(42.3%)

추가 이미지 OCR은 기존에 확보된 두 축이 새 3축 후보 안에 모두 존재하고, 허용 오차가 15mm 또는 3% 이내일 때만 후보가 된다. 기존 축과 새 축의 위치가 달라지는 경우에는 같은 이미지 문맥에 명시적인 W/D/H가 있어야 한다. 동일 점수의 3축 후보가 둘 이상이면 라인업/옵션 모호성으로 자동 반영하지 않는다.

OCR 오인식 `)( → ×`, `I/l → 숫자 1`은 범용 파서에서 적용하지 않는다. 여러 모델이 함께 있는 라인업 표에서 다른 제품의 숫자를 결합하는 오탐이 확인됐기 때문이다. 이 보정은 상품 옵션 또는 기존 축과 동일 모델임이 검증된 단계에서만 허용하며, 검증된 W/D/H 문장을 원 OCR보다 우선하는 별도 근거로 저장한다.

옵션+OCR 결합은 다음 조건을 모두 만족한 2개 상품만 적용했다.

- 모든 숫자형 사이즈 옵션이 OCR의 첫 축과 1:1 대응
- 각 옵션의 D/H가 공통
- New Mckinley Dinning Table: 1650/1800 × 900 × 700mm
- Morino Kotoba Desk: 1100/1200 × 600 × 710mm

연구·감사 테이블:

- `stg_dimension_ocr_attempt`
- `stg_dimension_ocr_result`
- `stg_dimension_option_inference`
- `source_ocr_dimension_backup`

고객용 엑셀은 이 단계에서 생성하지 않았다.

## 확정 대기 패턴: 에이스침대 INFO 대표 규격

2026-07-24 사용자 공동 검토를 위해 확정 적용 전 상태로 보존한다.

- 대상: 현재 `COMPARISON_PROVIDED`인 에이스침대 침대+매트리스 36개
- 공통 근거:
  - 대표 후보 원천이 `TARGETED_REGION_OCR`
  - OCR 문맥에 `INFO`, `모델명`, `침대 프레임 + 매트리스`,
    `가로`, `세로`, `헤드 높이`가 모두 존재
  - 단위가 명시된 `가로 × 세로 × 헤드 높이 mm` 완전값
  - 상품명의 사이즈 코드 `SS/DD/LQ/K/K3`와 후보의 옵션 코드가
    36개 모두 일치
- 비교상태로 남은 이유:
  - 같은 상세 이미지에서 매트리스 단품 규격과 적용 가능한 매트리스
    옵션표가 함께 검출됨
  - 36개에서 245개 비교 후보가 생성되어 상품당 평균 6.8개 후보
  - 현재 문맥 엔진은 대표값을 선택했지만 기존
    `COMPARISON_PROVIDED` 상태를 자동 승격하지 않는 원장 보존 정책이 적용됨
- 제안 규칙:
  - 위 공통 근거를 모두 만족하면 INFO 영역의 프레임 외형을 대표 규격으로
    선택
  - `매트리스 타입`, `OPTIONS`, `적용 가능한 매트리스` 문맥의 치수는
    대표값에서 제외하고 구성품·비교정보로 보존
- 적용 시 예상 전환: `비교정보제공 → OCR·규칙확정` 36개
- 결정 필요:
  - 침대+매트리스 세트의 대표 규격을 INFO의 프레임 외형
    `가로 × 세로 × 헤드 높이`로 확정할지 사용자 승인 필요
