# 복수 침대 상품의 세트 ID·옵션 ID 검증

> **보조 근거 문서**  
> 세트 ID 미확보와 `(product_id, option_id)`,
> `(product_id, component_seq)` 키 원칙은 계속 유효하다. 조합상품의
> 최종 N/S/Q/M/A/O 패턴과 상품별 현재 상태는
> [`조합상품_3D변환_패턴목록_2026-07-27.md`](./조합상품_3D변환_패턴목록_2026-07-27.md)를
> 기준으로 한다.

## 결론

현재 상품상세 API의 `optionId`는 세트 구성 ID가 아니라 색상 등 구매 옵션을
식별하는 상품 내부 옵션 ID다. 고객 요구사항의 `세트 구성 실제 ID`는 현재
원천에서 제공 불가로 판정한다.

복수 침대 10개에서 세트 구성을 직접 연결하는 구조화 `setId`는 확인되지
않았다. `productId`는 원본 판매상품의 그룹키로 사용할 수 있지만 이를 공식
세트 ID로 명명하거나 세트 구성 실제 제품 ID로 대체해서는 안 된다.

## 확인 원천

- 상품상세:
  `GET https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/goods/{productId}`
- 패키지 추천:
  `GET https://livingapi.lge.co.kr/itemsvc/ajax/v1/goods/packages`
- PDP JavaScript의 옵션 선택 로직
- 상세 HTML의 묶음구성 이미지 alt와 URL

## API 확인 결과

시몬스 복수 침대 9개 상품은 다음 구매 옵션만 가진다.

| optionId | optionName |
|---|---|
| 10001 | 라이트노체 |
| 10002 | 라이트오크 |
| 10003 | 베이지 |
| 10004 | 모카그레이 |
| 10005 | 내추럴블랙 |

같은 `optionId`가 서로 다른 복수 침대 상품에서 반복된다. 따라서
`optionId=10001`만으로는 어느 침대 세트인지 식별할 수 없다.

권장 옵션 키:

```text
(product_id, option_id)
```

확인된 다른 필드:

- `goodsCombinationDcInfo`: 10개 모두 `null`
- packages API `data`: 10개 모두 빈 배열
- `G26030036633`: 단일 재고 `optionId=00000`

## 상세 이미지의 묶음 코드

상세 HTML에는 구조화 ID가 아니라 이미지 파일명으로 묶음 구성이 표현된
사례가 있다.

| productId | 상세 이미지 파일명 예 |
|---|---|
| G25110023370 | `pack_QE+SS.jpg` |
| G25110023367 | `smartpack_frame+frame+101.jpg` |
| G25110023379 | `pack_vive_QE+SS.jpg` |
| G25110023380 | `pack_vive_QE+QE.jpg` |
| G25110023381 | `pack_vive_SS+SS.jpg` |

이 파일명은 보조 근거로 사용할 수 있지만 공식 세트 ID로 사용하면 안 된다.
`G25110023371`은 상품명이 퀸+퀸인데 상세 이미지 파일명은
`pack_SS+SS.jpg`라서 원천 간 불일치도 존재한다.

## 3D 데이터 모델 처리안

```text
set_id             = NULL / 미확보
source_product_id  = product_id
sales_option_key   = product_id + option_id
component_key      = product_id + component_seq
```

- `set_id`: 공식 원천이 없으므로 공란
- `product_id`: 원본 판매상품과 파생 구성품 행을 묶는 그룹키
- `option_id`: 해당 묶음의 색상 등 구매 선택값
- `component_seq`: Q 침대, SS 침대, 협탁처럼 3D 규격을 나눌 대상

별도의 `packageId`는 `/package` 전용 패키지 상품 ID이며, 현재 복수 침대
10개 상품과는 연결되지 않는다.
