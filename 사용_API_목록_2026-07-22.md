# 현재까지 사용한 API 목록

기준일: 2026-07-22

## 1. 홈스타일 대량 수집에 실제 사용한 API

대상은 비음영 홈스타일 60개 카테고리, 9,358개 고유 상품이다.

| 번호 | 구분 | Method | Endpoint | 주요 파라미터 | 사용 내용 | 실행 결과 |
|---:|---|---|---|---|---|---|
| 1 | 상품목록/PLP | GET | `https://livingapi.lge.co.kr/displaysvc/ajax/v1/shop/goods` | `filterSelType=SHOP`, `pageNum`, `pageSize`, `category3` | 카테고리별 상품 ID, 목록 정보, 카테고리-상품 소속관계 | 60개 카테고리 모두 HTTP 200, 목록행 9,628개, 중복 제거 9,358개 |
| 2 | 상품상세 | GET | `https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/goods/{productId}` | `epFlagYn=N` | 상품명, 브랜드, 카테고리, 가격·재고, 구매옵션, 옵션 스타일, 상품고시, 롤링 이미지, `detailInfo` HTML | 9,358/9,358 HTTP 200 |
| 3 | 공간추천 | GET | `https://livingapi.lge.co.kr/displaysvc/ajax/v1/collection/pdp/space-recommendation` | `goodsId={productId}` | 공간 콘텐츠, 공간명/스타일/해시태그, 포함 상품, 제품-공간 관계 | 9,358/9,358 HTTP 200; 실제 collection 보유 상품 269개, 연결 326건 |
| 4 | 패키지 추천 | GET | `https://livingapi.lge.co.kr/itemsvc/ajax/v1/goods/packages` | `goodsId={productId}` | 해당 상품이 포함된 홈스타일 패키지 추천, `packageId`, 패키지명/설명 | 9,358/9,358 HTTP 200; 비어 있지 않은 응답은 5개 상품 |
| 5 | 공개 상품 Q&A 목록 | GET | `https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/qna-list` | `goodsId`, `qnaType`, `isMyInquiry=false`, `excludeSecret=true`, `pageNum`, `pageSize` | 공개 질문/답변 목록, 치수·재질·설치·구성품 보조 근거 | 9,358/9,358 목록 호출 HTTP 200, 저장된 공개 Q&A 433건 |
| 6 | 공개 상품 Q&A 상세 | GET | `https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/qna-list/{inquiryId}` | `isNotice=false` | 질문 본문, 답변 본문, Q&A 유형·등록일 | 대량 실행에서는 상품별 공개 Q&A 앞 3건까지 상세 조회 |

### 상품상세 API에서 실제 읽은 주요 필드

- `productId`, `productName`, `brandName`, `category`
- `images[].imageUrl`, `imageAlt`, 정렬 정보
- `purchaseOptions`, `productStock`: 옵션 스타일·옵션값·재고
- `productNotification.items`: 크기/치수/규격, 색상, 소재, 구성품 등
- `detailInfo`: 공급사 상세 HTML과 상세 이미지 URL
- 가격, 배송, 설치 관련 구조화 필드

### 공간추천 API에서 실제 읽은 주요 필드

- `collections[].exhibitionId`
- `title`, `subTitle`, `category`
- `hashtags[].tagLabel`, `tagCd`
- `products[].productId`, `productName`
- 공간 콘텐츠 내 상품 관계와 배치 문맥

### 패키지 API 해석 주의

이 API는 일반적인 세트상품의 실제 구성 상품 ID를 반환하는 API가 아니다.
현재 응답은 해당 상품이 포함된 홈스타일 추천 패키지 목록에 가깝다.
따라서 고객 요청의 `세트 모델ID/프로덕트ID에 포함된 실제 제품 ID 리스트`를
이 API만으로 확정할 수 없다.

## 2. API가 아닌 함께 사용한 홈스타일 원천

| 구분 | URL/장치 | 사용 내용 |
|---|---|---|
| PDP HTML | `https://homestyle.lge.co.kr/item?productId={productId}` | title/meta, JSON-LD, `FAQPage`, 화면 텍스트, 치수 문맥 |
| 대표·롤링 이미지 | 주로 `https://static-store.lge.co.kr/goods/...` | 대표 이미지 및 여러 장의 롤링 이미지 URL |
| 공급사 상세 이미지 | `detailInfo`에 포함된 여러 외부 CDN URL | 이미지 내 규격표·재질·주의사항 OCR |
| OCR | `Windows.Media.Ocr/ko` 로컬 장치 | API/HTML에 없는 이미지 텍스트 추출; 대량 실행 주 엔진 |
| OCR 비교 | PaddleOCR, Tesseract.js | 일부 표본 품질 비교용; 외부 클라우드 OCR API가 아님 |

홈스타일에는 별도의 FAQ API를 사용하지 않았다. FAQ는 PDP HTML 안의
`application/ld+json` 형식 `FAQPage`에서 파싱했고, 고객 상품 Q&A만 위의
Q&A API를 사용했다.

## 3. 가전 4개 PoC에서 사용한 API

가전 대량 수집이 아니라 TV·냉장고·워시타워·에어컨 4개 표본 검증에 사용했다.

| 번호 | 구분 | Method | Endpoint | 주요 파라미터/Body | 결과와 사용 판단 |
|---:|---|---|---|---|---|
| 1 | 가전 상품 조회 | POST | `https://apiv2.lge.co.kr/itemsvc/ajax/v1/product/retrieveDealProduct` | `modelId`, `modelStatusCode=ACTIVE` | 4개 표본 호출. 응답은 정상이었으나 고객 요청의 핵심 스펙은 부족해 공식 PDP HTML을 주 원천으로 사용 |
| 2 | 공식 가전 FAQ | GET | `https://apiv2.lge.co.kr/itemsvc/ajax/v1/model/faq/{modelId}` | `modelId` | 4개 표본 모두 HTTP 200. 카테고리 공통 FAQ가 포함되므로 상품 확정값이 아닌 보조 근거로 사용 |
| 3 | 공개 가전 Q&A | POST | `https://www.lge.co.kr/mkt/api/qna/retrieveQnaList` | query: `modelId`, `page`; body: `questionTypeCode=ALL`, `excludePrivate=Y`, `myQna=N` | 4개 표본 모두 HTTP 200. 정확한 모델/상품 Q&A만 보조 근거로 사용하고 형제 모델 Q&A는 직접 입력에서 제외 |

가전 FAQ/Q&A 검증에서는 FAQ 216건, 공개 모델그룹 Q&A 159건을 조회했다.
홈스타일 4개 공개 Q&A 30건까지 합친 8개 표본 전체에서는 정확한
상품·모델 범위 121건만 후보 근거로 두었고, 가전 관련 모델 68건은 상품별
직접 채움에서 제외했다.

## 4. 초기에 호출했지만 현재 대량 수집에서는 교체한 Endpoint

초기 8개 PoC 코드에는 다음 경로가 포함돼 있었다.

- `https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/space-recommendation`
- `https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/packages`

초기 경로는 404 또는 유효 데이터 부족 사례가 있어 최종 대량 수집에서는
각각 다음 경로로 교체했다.

- 공간: `displaysvc/ajax/v1/collection/pdp/space-recommendation`
- 패키지: `itemsvc/ajax/v1/goods/packages`

## 5. 개인정보 및 사용 제한

- Q&A는 `excludeSecret=true`, `isMyInquiry=false`로 공개 건만 조회했다.
- 작성자·첨부파일은 저장하지 않았고 전화번호·이메일은 마스킹했다.
- 고객 질문은 오기나 추측일 수 있으므로 공식 답변 및 상품상세 원천과
  교차검증할 때만 보조 근거로 사용했다.
- API 응답 HTTP 200은 요구 필드가 실제 채워졌다는 뜻이 아니다. 예를 들어
  패키지 API는 9,358건 모두 200이지만 실제 데이터는 5건만 비어 있지 않았다.

## 6. 구현 근거

- `bulk_homestyle_collect.py`: 홈스타일 목록·상세·공간·패키지·Q&A·HTML 수집
- `parse_faq_qna.py`: 홈스타일/가전 FAQ·Q&A 검증
- `collect_full_poc_sources.py`: 초기 8개 API/HTML PoC
- `bulk_homestyle_ocr.py`: 상세 이미지 다운로드와 로컬 OCR
