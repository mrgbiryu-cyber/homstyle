# 홈스타일·가전 정보 구성 Depth 및 사용 장치 정리

- 정리 기준일: 2026-07-21
- 검증 범위: 대표 상품 8개(홈스타일 4개, LG 가전 4개)
- 목적: 지금까지 어떤 원천을 어느 깊이까지 확인했고, 어떤 수집·파싱·OCR·검증 장치를 사용했는지 재현 가능한 형태로 기록
- 주의: 아래 API는 공개 PDP가 사용하는 조회 경로를 분석한 것이며, 공식 외부 연동 계약 API라는 의미는 아니다.

## 1. 검증 대상

| 구분 | 상품 ID | 대표 제품군 |
|---|---|---|
| 홈스타일 | `G25070005743` | 소파 |
| 홈스타일 | `G25100020496` | 러그 |
| 홈스타일 | `G25070001871` | 팬던트 조명 |
| 홈스타일 | `G25070006112` | 액자 |
| 가전 | `OLED48C6KNA` / `MD10770851` | TV |
| 가전 | `G646GBB031` / `MD10780848` | 냉장고 |
| 가전 | `WA2525YMZF` / `MD10576829` | 워시타워 |
| 가전 | `SQ06GJ1WFS` / `MD10766829` | 에어컨 |

## 2. 전체 Depth 구조

Depth는 단순히 많이 호출했다는 뜻이 아니라, 앞 단계에 값이 없을 때 다음 단계로 내려가는 우선순위다.

| Depth | 계층 | 확인·처리 내용 | 주 용도 | 값의 성격 |
|---:|---|---|---|---|
| D0 | 입력·범위 | 상품 ID, PDP URL, 홈스타일/가전 구분, 제품군·적용성 | 수집 대상과 필수 필드 결정 | 입력값 |
| D1 | PDP 응답 | 공개 PDP 원본 HTML, 상태코드, 응답 시각, 해시 | 원본 스냅샷과 재현 근거 확보 | 원천 사실 |
| D2 | HTML 구조 | `<title>`, 메타 태그, `og:image`, 표시 텍스트, 컴포넌트 속성 | 상품명·설명·대표 이미지·모델 ID 보강 | 원천 사실 |
| D3 | PDP HTML 묶음 | 메타·본문, JSON-LD `Product`/`BreadcrumbList`/`FAQPage`, PDP가 비동기로 붙이는 FAQ/Q&A | 상품 기본정보와 공개 질문답변을 함께 구성 | 원천 사실 또는 보조 근거 |
| D4 | 상품 API·전송 경로 | 핵심 상품 API와 PDP HTML 묶음이 호출하는 FAQ/Q&A 조회 경로 | 옵션·고시·가격·배송 및 HTML 콘텐츠 공급 | 원천 사실 또는 전송 수단 |
| D5 | 이미지 자산 | API/HTML/상세설명에서 이미지 URL 탐색, 역할 분류, 다운로드 | 이미지에만 존재하는 규격·소재·주의정보 준비 | 원천 이미지 |
| D6 | OCR | Windows OCR, PaddleOCR, Tesseract.js 비교 | 이미지 내부 문자·숫자 보강 | 인식값, 검증 필요 |
| D7 | 정규화·병합 | 단위, W×D×H, 색상, 카테고리, 설치·배치 값 표준화 | 고객 요청 필드로 변환 | 사실+정규화 |
| D8 | 규칙·추론 | 카테고리 적용성, 공간·배치·스타일·동의어 관계 | 원천에 직접 없는 추천용 필드 생성 | 추론값 |
| D9 | 품질·출력 | 출처, 신뢰도, 정확 모델 범위, 결측/N/A, 통계, Excel/JSON | 검수와 대량 실행 관리 | 판정·메타데이터 |

## 3. Depth별 실제 수행 내용

### D0. 입력과 적용성 분류

- `productId` 또는 가전 `modelId`를 기본 키로 사용했다.
- 홈스타일과 가전은 원천 구조가 달라 파서를 분리했다.
- 동일한 요구 항목이라도 제품군에 따라 `필수`, `선택`, `해당 없음`을 구분했다.
  - `미확보`: 해당 제품군에 필요한 값이지만 원천에서 찾지 못함
  - `해당 없음(N/A)`: 제품군 특성상 적용되지 않음
  - 예: 바닥 배치 소파의 벽면부착 추천 높이는 N/A
- 고객의 논리 요구사항은 요청 1의 10개 항목과 요청 2의 7개 항목, 총 17개다.
- 최종 시트에서는 분류를 중/소카테고리로 나누고 요청 2의 하위 태그를 펼쳐 총 37개 요청 데이터 열로 확장했다.

### D1. PDP 원본 HTML 수집

- 8개 PDP의 원본 HTML을 `poc_full_run/raw_html/`에 저장했다.
- 응답 상태, 길이, SHA-256, 수집 시점과 원본 URL을 기록했다.
- 동적 화면을 눈으로만 확인하지 않고, 재처리가 가능한 원본 스냅샷을 남겼다.
- 가격·재고·프로모션·Q&A 건수는 동적 값이므로 스냅샷 시점을 함께 봐야 한다.

### D2. HTML·컴포넌트 파싱

- 표준 라이브러리 `HTMLParser`, 정규식, JSON 파서를 사용했다.
- 추출 대상:
  - 문서 제목과 메타 설명
  - `og:image`
  - 표시 텍스트와 규격 관련 문자열
  - 이미지의 `src`, `data-src`, `data-original`, `data-pc-src` 등 지연 로딩 속성
  - 가전 PDP 컴포넌트의 `modelId`, `data-ajax` 경로
- JavaScript 번들에서 화면이 실제 호출하는 FAQ/Q&A API 경로와 파라미터를 역추적했다.

### D3. PDP HTML 묶음(JSON-LD + FAQ/Q&A)

운영 기준에서 HTML은 최초 HTML 문자열만 뜻하지 않는다. PDP 화면을 완성하기 위해 HTML 컴포넌트가 비동기로 불러오는 공개 FAQ/Q&A까지 하나의 `PDP_HTML_FAQ_QNA` 묶음으로 취급한다.

- 가전 PDP에서는 `Product`와 `BreadcrumbList`가 핵심 상품 원천으로 작동했다.
- `Product`에서 상품명, SKU, 브랜드, 이미지, 가격, 평점 등을 추출했다.
- `BreadcrumbList`에서 카테고리 경로를 추출했다.
- `FAQPage`의 `Question`/`acceptedAnswer`를 정규화했다.
- HTML에 직접 포함되지 않은 현재 Q&A도 PDP 컴포넌트의 조회 경로로 수집해 동일 HTML 묶음에 연결했다.
- 저장 HTML의 FAQPage 건수:
  - 홈스타일 고객 Q&A 스냅샷 21건
  - 가전 공식 제품군 FAQ 108건
  - 합계 129건
- 기존 심층 상품 파서는 `Product`와 `BreadcrumbList` 중심이었으나, 현재는 `deep_source_inventory.json`의 각 상품 `html_fields.faq_qna_context`에 FAQ/Q&A를 필수 연결한다.
- 질문·답변 전체 레코드는 중복 저장하지 않고 `faq_qna_probe.json`을 근거 저장소로 참조한다.

### D4. 상품 API와 PDP 비동기 전송 경로

아래 표는 실제 HTTP 전송 경로를 구분하기 위한 것이다. FAQ/Q&A의 논리적 소속은 별도 API 계층이 아니라 D3의 PDP HTML 묶음이다.

#### 홈스타일

| API | 결과 | 활용 |
|---|---|---|
| `GET https://livingapi.lge.co.kr/itemsvc/ajax/v1/pdp/goods/{productId}?epFlagYn=N` | 4개 모두 HTTP 200 | 상품명, 브랜드, 카테고리, 옵션, 상품정보고시, 가격·배송·판매 정보, 이미지 |
| `GET .../itemsvc/ajax/v1/pdp/qna-list` | 4개 모두 HTTP 200 | 공개 상품 Q&A 목록, 페이지, 총건수 |
| `GET .../itemsvc/ajax/v1/pdp/qna-list/{inquiryId}` | 공개 건 HTTP 200 | 질문 본문과 답변 상세 |
| 8개 전체 수집기의 `space-recommendation` 경로 | HTTP 404 | 현재 경로 재탐색 필요, 직접 값으로 사용하지 않음 |
| 8개 전체 수집기의 `packages` 경로 | HTTP 404 | 현재 경로 재탐색 필요, 세트 ID를 만들지 않음 |

- 홈스타일 공개 Q&A: 소파 29건, 러그 0건, 조명 1건, 액자 0건으로 총 30건을 수집했다.
- 비밀글과 내 문의는 제외했다.

#### LG 가전

| API | 결과 | 활용 |
|---|---|---|
| `POST https://apiv2.lge.co.kr/itemsvc/ajax/v1/product/retrieveDealProduct` | 4개 모두 HTTP 200이나 핵심 모델 데이터 없음 | 호출 성공과 데이터 존재 여부를 분리 판정 |
| `GET https://apiv2.lge.co.kr/itemsvc/ajax/v1/model/faq/{modelId}` | 4개 모두 HTTP 200 | 공식 제품군 FAQ 108건 |
| `POST https://www.lge.co.kr/mkt/api/qna/retrieveQnaList?modelId={modelId}&page={page}` | 4개 모두 HTTP 200 | 비공개 제외 상품 Q&A 159건 |

- 가전 FAQ API 108건은 HTML의 FAQPage 108건과 제목 기준 모두 일치하여 신규 필드 증가가 아니라 안정적 재수집 경로로 판단했다.
- 가전 Q&A API는 같은 모델 그룹의 형제 `modelId`를 함께 반환했다.
  - 홈스타일 goodsId 및 가전 정확 modelId 후보: 121건
  - 가전 형제 모델이라 직접 입력에서 제외: 68건
- 고객 질문은 오류나 추측을 포함할 수 있어 질문만으로 사실값을 만들지 않는다.
- 정확 모델의 답변 완료 Q&A도 공식 상품 API/스펙의 보조 근거로 사용한다.

### D5. 이미지 자산 탐색과 선별

- 이미지 원천:
  - 홈스타일 상품 API 갤러리
  - `detailInfo` 내부 상세 이미지
  - PDP HTML 이미지 태그와 지연 로딩 속성
  - 가전 모델 ID가 URL에 포함된 PDP 이미지
- 이미지 URL을 절대경로로 정규화하고 중복 제거했다.
- 선택 우선순위:
  1. 대표·갤러리 이미지
  2. URL/대체텍스트에 `size`, `spec`, `dimension`, `mm` 등 규격 신호가 있는 이미지
  3. 상세·기능·설명 이미지
  4. 일반 이미지
- 상품별 최대 8장을 OCR 후보로 선정했다.
- 이미지 매니페스트 63행 중 62장을 정상 다운로드했다.
- 실제 파일은 `poc_full_run/images_flat/`에 저장했다.

### D6. OCR 장치 비교

동일한 62개 이미지를 대상으로 세 엔진을 비교했다. 전체 문장 CER이 아니라, 상품 필드에 중요한 숫자 토큰 28개와 핵심 문구 13개의 정확 재현율을 측정했다.

| 엔진 | 실행 장치·설정 | 숫자 토큰 재현율 | 핵심 문구 재현율 | 빈 이미지 오탐률 | 평균 속도 | 판정 |
|---|---|---:|---:|---:|---:|---|
| PaddleOCR PP-OCRv5 | Python 3.11 계열, PaddleOCR 3.7.0/Paddle 3.3.0, CPU, 한국어 모바일 검출·인식, MKLDNN 비활성 | 100.0% | 100.0% | 14.3% | 7,396ms/장 | 정확도 1위, 오탐 필터 필요 |
| Windows OCR | PowerShell WinRT `Windows.Media.Ocr`, `ko`와 `en-US` 이중 패스, 결과 병합 | 96.4% | 76.9% | 3.6% | 622ms/장 | 속도·정확도 균형 |
| Tesseract.js 7.0.0 | Node.js/WebAssembly, `kor+eng`, LSTM_ONLY, PSM.AUTO | 25.0% | 69.2% | 0.0% | 526ms/장 | 숫자 규격 회수율이 낮아 3순위 |

Windows OCR 전처리:

- 최대 너비 2,200px로 축소
- 높이 2,200px 단위 타일 분할
- HighQualityBicubic 리사이즈
- 한국어/영어 각각 실행 후 텍스트 병합

PaddleOCR 설정:

- `PP-OCRv5_mobile_det`
- `korean_PP-OCRv5_mobile_rec`
- `text_det_limit_side_len=1280`
- 인식 점수 0.5 이상만 유지
- CPU 실행, MKLDNN 비활성

Tesseract 관련 제한:

- 네이티브 Tesseract 5.5, tesserocr, conda, WSL 경로를 검토했으나 UAC·컴파일러·저장소 제약으로 실행하지 못했다.
- 따라서 실제 비교 대상은 네이티브 CLI가 아니라 Tesseract.js WebAssembly 포트다.

OCR 채택 규칙:

- API/HTML에 정확한 값이 있으면 OCR로 덮어쓰지 않는다.
- OCR은 API/HTML 결측을 보강할 때만 사용한다.
- 숫자·단위·제품명 문맥이 함께 맞아야 채택한다.
- 다른 상품, 기존 제품, 비교 모델을 설명한 숫자는 제외한다.
- 빈 이미지에서 나온 문자는 오탐 후보로 처리한다.

현재 대량 OCR은 상세 이미지 전체 URL을 수집하더라도 우선순위 1장만 읽는
구조다. `전체 이미지 목록화 + 유효 후보 순차 OCR + 규격 충족 시 중단`은
다음 개발 항목 `ND-IMG-OCR-01`로 등록했다. 상세 설계는
`정규화_항목_및_다음개발_백로그.md`에 기록한다.

### D7. 필드 정규화와 병합

- 내부 심층 검증 필드: 37개 × 8상품 = 296개 셀
- 주요 정규화:
  - `W×D×H` 순서와 mm 단위
  - 브랜드·카테고리 경로
  - 색상 원문과 표준 색상
  - 설치 타입과 배치 위치
  - 구성품과 세트 실제 제품 ID의 분리
  - 가격·재고·배송의 수집 시점 분리
- 소스 병합 우선순위:
  1. 정확한 상품 ID의 구조화 상품 API
  2. 정확한 SKU/modelId의 HTML JSON-LD 또는 명시 스펙
  3. 정확 모델의 공식 FAQ·공식 답변
  4. OCR로 확인한 상세 이미지 텍스트
  5. 카테고리 규칙
  6. 이미지·문장 기반 추론
- 각 값에 `source`, `evidence`, `confidence`, `status`를 붙이는 구조를 사용했다.

### D8. 규칙·추론 장치

- 카테고리 → 추천 공간
- 카테고리 → 배치 가능 위치
- 가전 카테고리·명시 문구 → 설치 타입
- 색상 원문 → 표준 색상·동의어
- 상품 설명 → 사용 목적·설명서 태그
- 제품명·이미지·설명 → 디자인 스타일 후보
- FAQ/Q&A 문장 → 의미 검색용 보조 코퍼스

현재 사용한 것은 규칙·키워드·정규식 기반 후보 생성이다. 전용 비전 모델이나 임베딩 검색 인덱스를 8개 전체에 생산 수준으로 적용한 것은 아니다.

추론값은 반드시 다음과 같이 구분한다.

- `EXACT`: 원천에 명시된 값
- `PARTIAL`: 일부만 확보되거나 참고 문구뿐인 값
- `INFERRED`: 규칙·문맥·이미지로 추론한 값
- `MISSING`: 필요하지만 미확보
- `N_A`: 해당 제품군에 적용되지 않음

### D9. 품질 판정·통계·출력

심층 37필드 기준 단계별 사용 가능률:

| 단계 | 사용 가능 셀 | 사용 가능률 | 이전 단계 대비 증가 |
|---|---:|---:|---:|
| 연구 비교용 API 단독 — 운영 사용 금지 | 139/296 | 47.0% | 비교 기준일 뿐 완성 방식이 아님 |
| 운영 기본선: API + PDP HTML/JSON-LD + FAQ/Q&A | 224/296 | 75.7% | +85셀 |
| 운영 기본선 + Windows OCR | 226/296 | 76.4% | +2셀 |
| 운영 기본선 + PaddleOCR | 226/296 | 76.4% | 사용 가능 셀 동일, 확정/부분 판정 품질 개선 |

해석:

- API 단독은 홈스타일과 가전의 원천 구조가 다르므로 운영 가능한 수집 방식이 아니다.
- 필수 운영 기본선은 `상품 API + PDP HTML/JSON-LD + FAQ/Q&A`다.
- 가장 큰 증가는 PDP HTML 묶음의 심층 파싱에서 발생했다.
- OCR은 필수지만 전체 매핑률을 크게 올리는 주 원천은 아니고 이미지 결측 보강 장치다.
- FAQ/Q&A 추가 API는 현재 비어 있는 고객 요청 필드를 새로 확정한 셀은 0개였다.
- FAQ/Q&A는 냉장고 사양 차이, 워시타워 구성·설치·관리, 에어컨 규격·색상 등 기존 값의 근거를 강화했다.

## 4. 사용한 실행 장치·소프트웨어

| 구분 | 장치·소프트웨어 | 사용 목적 |
|---|---|---|
| 운영 환경 | Windows, PowerShell | 파일·HTTP·WinRT OCR 실행 |
| 수집기 | Python `urllib.request`, `requests` | PDP/API GET·POST 조회 |
| HTML 파서 | Python `html.parser.HTMLParser`, 정규식 | 메타·텍스트·이미지·JSON-LD 블록 추출 |
| JSON 파서 | Python `json` | API/JSON-LD 응답 정규화 |
| 이미지 처리 | System.Drawing, Pillow | 이미지 크기 확인, 리사이즈·타일링 |
| OCR 1 | Windows.Media.Ocr | 빠른 한국어/영어 이중 패스 |
| OCR 2 | PaddleOCR PP-OCRv5 | 고정밀 한국어·숫자 인식 |
| OCR 3 | Tesseract.js 7.0.0 | 제3 엔진 비교와 보수적 교차확인 |
| 규칙 엔진 | 정규식, 키워드 사전, 제품군 매핑 | WDH·색상·설치·공간·태그 정규화 |
| 검증 장치 | 정확 토큰/문구 골드셋, 빈 이미지 오탐셋 | OCR 재현율·오탐 비교 |
| 저장 형식 | JSON, 원본 HTML, 이미지 | 원천·중간 결과·근거 보존 |
| Excel 생성 | Python 표준 라이브러리 기반 OOXML 작성기 | 통계·요청색상·결측색상 포함 시트 생성 |

네트워크 환경 참고:

- Python `requests` 환경에 사내 프록시 CA가 설치되지 않아 검증 실행에서는 공식 `lge.co.kr` 계열 호스트에 한해 TLS 검증을 비활성화했다.
- 운영에서는 TLS 검증을 끄지 말고 사내 CA를 설치해야 한다.

## 5. 주요 실행 스크립트와 역할

| 파일 | 역할 | 주 산출물 |
|---|---|---|
| `collect_full_poc_sources.py` | 8개 상품 API/HTML/이미지 수집 | `source_results.json`, `raw_api/`, `raw_html/`, `image_manifest.json`, `images_flat/` |
| `run_windows_ocr.ps1` | Windows OCR 단일 언어 실행 | `ocr_ko.json`, `ocr_en.json` |
| `run_paddle_ocr.py` | PaddleOCR 실행 | `ocr_paddle_ko.json` |
| `run_tesseract_js.js` | Tesseract.js 실행 | `ocr_tesseract_js.json` |
| `build_ocr_benchmark.py` | OCR 3종 재현율·오탐·속도 비교 | `ocr_engine_benchmark.json` |
| `deep_parse_sources.py` | API/HTML/OCR 심층 필드 병합과 단계 통계 | `deep_source_inventory.json` |
| `parse_faq_qna.py` | HTML FAQPage와 추가 FAQ/Q&A API 파싱 | `faq_qna_probe.json` |
| `build_bulk_check_template.py` | 제품군 대량점검 양식·요청별 색상·통계 | 버전별 대량점검 Excel |
| `build_customer_requested_fields.py` | 연구용 열을 제외하고 고객 요청 필드만 출력 | `상품ID별_17개 항목 추출 샘플(가전4개,홈스타일4개).xlsx` |

실행 주의:

- `collect_full_poc_sources.py`는 현재 `poc_full_run` 폴더를 새로 만드는 구조이므로 재실행 전에 기존 결과를 백업하거나 수집기 출력을 버전 폴더로 바꿔야 한다.
- 가격·재고·배송·Q&A는 실행 시점마다 달라질 수 있다.

## 6. 주요 산출물 위치

| 산출물 | 위치 | 내용 |
|---|---|---|
| 원본 수집 요약 | `poc_full_run/source_results.json` | 8개 상품 API/HTML/이미지 수집 결과 |
| 이미지 매니페스트 | `poc_full_run/image_manifest.json` | 이미지 URL, 파일, 다운로드 결과, 해시 |
| OCR 비교 | `poc_full_run/ocr_engine_benchmark.json` | 엔진별 정확 토큰·문구 재현율, 오탐, 속도 |
| 심층 필드 통계 | `poc_full_run/deep_source_inventory.json` | 37필드 × 8상품 단계별 매핑 결과 |
| FAQ/Q&A 결과 | `poc_full_run/faq_qna_probe.json` | HTML FAQPage, 추가 API, 정확/형제 모델 범위, 후보 신호 |
| 고객용 샘플 | `상품ID별_17개 항목 추출 샘플(가전4개,홈스타일4개).xlsx` | 홈스타일/가전 수량 요약과 요청 필드만 포함한 8개 샘플 |
| 확장 고객용 양식 | `상품ID별_17개_요구필드_확장양식_롤링이미지_숫자규격.xlsx` | 대표/롤링 이미지 개별 URL과 숫자형 W/D/H, 복수 규격 상세 시트 |

## 7. 현재까지 확인된 한계

1. 세트 구성 실제 제품 ID는 구성명이나 구성품 문장만으로 생성할 수 없다. 공식 패키지/상품 관계 원천이 필요하다.
2. 벽면부착 추천 높이는 안전과 설치 품질에 영향을 주므로 일반 FAQ나 이미지 추정값으로 채우지 않는다.
3. 조명 Q&A에는 전구·전선 포함 질문이 있으나 답변이 고객센터 문의 안내라 구성 사실을 확정할 수 없다.
4. 가전 Q&A는 형제 모델을 섞어 반환하므로 요청 modelId와 응답 modelId를 반드시 비교해야 한다.
5. FAQ는 카테고리 공통 설명이 많아 현재 개별 상품의 확정 스펙으로 바로 쓰면 안 된다.
6. OCR 최고 점수 엔진도 빈 이미지 오탐이 있으므로 숫자·단위·제품 문맥을 함께 검증해야 한다.
7. 공간 콘텐츠의 실제 공간 크기와 공간 내 제품 관계는 공간 이미지·좌표·제품 목록 원천이 있어야 정확하게 생성할 수 있다.
8. 개인 구매·선호·보유·CRM은 공개 PDP에서 수집하지 않으며 별도 내부 데이터 권한과 적법한 결합 기준이 필요하다.
9. 공개 PDP 내부 API는 스키마·경로가 바뀔 수 있으므로 대량 운영 전 공식 데이터 피드 또는 호출 정책 확인이 필요하다.
10. 콘텐츠·이미지의 대량 저장과 재사용은 서비스 이용 조건과 내부 권한을 별도로 확인해야 한다.

## 8. 대량 처리 시 권장 최종 흐름

1. 제품군 목록에서 음영 제외 카테고리와 적용성 규칙을 확정한다.
2. 상품 ID 기준으로 홈스타일/가전 수집기를 분기한다.
3. 핵심 API와 PDP HTML을 동시에 저장한다.
4. JSON-LD와 비동기 FAQ/Q&A를 파싱한다.
5. API/HTML 결측이 있는 상품만 OCR 후보 이미지를 선택한다.
6. Windows OCR을 빠른 1차로 사용하고, 중요 결측은 PaddleOCR로 재확인한다.
7. 정확 모델·형제 모델·카테고리 공통 정보를 분리한다.
8. `EXACT/PARTIAL/INFERRED/MISSING/N_A`와 근거를 저장한다.
9. 고객용 시트에는 요청 필드만 노출하고 연구·오탐·엔진 비교 정보는 별도 근거 JSON에 보관한다.
10. 맨 앞 통계 시트에서 상품 수, 요청 필드 수, 가능/부분/불가/N/A와 소스별 증가량을 집계한다.

## 9. 현재 결론

- 정보 구성의 최소 주축은 `상품 API + PDP HTML/JSON-LD + FAQ/Q&A`다.
- OCR은 상세 이미지에만 있는 숫자·문구를 보강하는 2차 장치다.
- FAQ/Q&A API는 매핑률을 바로 올리기보다 사양·설치·사용·관리의 근거 품질을 높이는 장치다.
- 제품별 확정값을 만들 때는 상품 ID/modelId 일치, 답변 완료 여부, 원천 명시 여부를 먼저 본다.
- 대량 처리에서는 상품 API와 PDP HTML/FAQ/Q&A를 항상 실행하고, 이 기본선에 결측이 있을 때 OCR을 선택적으로 실행하는 방식이 효율적이다.

## 10. 롤링 이미지·숫자 규격 확장 양식

현재 확장 양식은 3개 시트로 구성한다.

1. `00_요약`: 상품 수, 논리 요구사항 수, 최대 롤링 이미지 수, 규격 단위와 색상 규칙
2. `01_상품별_요구필드`: 상품당 한 행, 대표 이미지와 롤링 URL을 각각 별도 컬럼으로 출력
3. `02_규격_상세`: 구성품·사이즈 옵션별 한 행, W/D/H를 숫자형 셀로 출력

이미지 규칙:

- `대표 이미지 URL`과 `롤링 이미지 수`를 별도 저장한다.
- `롤링 이미지 URL 01…N`은 실제 PDP 노출 순서를 유지한다.
- 롤링 01은 대표 이미지를 포함하므로 대표 URL과 동일할 수 있다.
- 가전은 같은 슬라이드의 small/medium/large 중복을 제거하고 `medium` URL 하나만 사용한다.
- 열 수 N은 대상 데이터의 최대 롤링 이미지 수에 따라 자동 확장한다. 현재 8개 샘플은 최대 15장이다.

규격 규칙:

- 기준 단위는 mm다.
- 메인 시트 컬럼은 `W (mm)`, `D (mm)`, `H (mm)`로 분리한다.
- 값 셀에는 `mm`, `cm`, `W` 같은 문자를 넣지 않고 정수 또는 소수 숫자만 저장한다.
- cm 원천은 mm로 환산한 후 저장한다.
- 복수 구성품·옵션은 `02_규격_상세`에서 행을 나눈다.
  - 소파 본체와 스툴: 2행
  - 러그 3개 사이즈: 3행
- 일부 축만 확보되면 확보된 축은 숫자로 저장하고 나머지는 빈칸+분홍색, 상태는 `부분확보`로 표시한다.
- 제품 외형 치수가 없으면 W/D/H는 빈칸+분홍색, 상태는 `미확보`로 표시한다.
- 액자의 `3×3인치`처럼 사진·화면·용량 규격인 값은 제품 외형 W/D/H로 변환하지 않는다.
