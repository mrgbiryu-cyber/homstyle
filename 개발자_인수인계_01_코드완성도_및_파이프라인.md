# 홈스타일 상품정보 수집·규격 OCR 개발자 인수인계

기준일: 2026-07-24  
현재 대상: 홈스타일 비음영 카테고리 상품 9,358개  
DB: `homestyle_bulk_run/homestyle_bulk.sqlite`

## 1. 결론과 코드 완성도

현재 코드는 **수집 완료 DB에서 규격을 재평가하고 Excel을 다시 만드는
PoC/배치 코드로는 동작**한다. 다만 **빈 DB와 새 PC에서 한 명령으로 처음부터
재구축하는 제품화 코드**는 아직 아니다.

| 구분 | 상태 | 확인 내용 |
|---|---|---|
| 현재 DB 조회·통계 | 완료 | 9,358개 원장 합계, 중복·고아 행 0 |
| 현재 DB 후처리 재실행 | 완료 | 원장 2회 실행 변동 0건 |
| 규격 파서 회귀검사 | 완료 | 21개 단위 테스트 통과 |
| 사용자 피드백 회귀검사 | 완료 | 17/17 통과 |
| Excel 재생성·검증 | 완료 | 9,358행, 비교 후보 17,580개, 정렬 오류 0 |
| API/HTML/Q&A 수집 | 완료 | 현재 DB 9,358개 모두 HTTP 200 |
| OCR 배치 | 완료 | 현재 데이터 산출에 필요한 실행 결과 존재 |
| 빈 DB 최초 구축 | 미완성 | 필수값 스테이징과 규격 원장의 순환 의존성 |
| 단일 파이프라인 실행기 | 미완성 | 단계별 Python/PowerShell 스크립트를 수동 실행 |
| 설치 명세 | 미완성 | `requirements.txt`·환경 잠금 파일 없음 |
| 신규 카테고리 입력 | 부분 | 현재는 `source_scope_cache.json`에 의존 |
| 운영 서비스/API | 미구현 | SQLite+배치+Excel 구조이며 서버 API는 없음 |

### 현재 발견된 필수 개선사항

#### P0. 빈 DB 부트스트랩 순환 의존성 해소

- `build_dimension_classification_master.py`는
  `stg_mandatory_pass`를 입력으로 사용한다.
- `build_mandatory_pass_staging.py`는
  `build_homestyle_bulk_workbook.build_rows()`를 호출한다.
- 현재 `build_rows()`는 규격 원장 뷰를 조회한다.
- 규격 원장은 분류 마스터가 먼저 있어야 만들어진다.

현재 데이터베이스에는 모든 중간 테이블이 있어 문제가 없지만, 빈 DB에서는
이 순환을 끊는 초기화 모드가 필요하다.

권장 수정안:

1. `build_mandatory_pass_staging.py --bootstrap`을 추가한다.
2. bootstrap에서는 API/HTML/OCR 기본값만으로 최초 필수값을 계산한다.
3. 분류 마스터와 규격 원장을 생성한다.
4. 최종 필수값 스테이징을 다시 계산한다.

또는 `load_dimension_resolution_export()`가 원장 뷰가 없을 때 빈 결과를
반환하도록 하고, 최초 스냅샷과 최종 스냅샷을 명확히 구분한다.

#### P0. 과거 잠금 옵션 14행 정리

`fact_dimension_resolution_option`에는 오탐 잠금이 해제된 14개 상품의 과거
옵션 행이 남아 있다. authoritative 원장과 Excel에서는 사용하지 않지만,
테이블을 직접 조회하면 혼동될 수 있다.

원인:

- `build_dimension_resolution_ledger.py`는 상품이 잠길 때 기존 옵션을
  삭제·재삽입한다.
- 잠금 상태에서 비교/무후보 상태로 내려갈 때는 옵션을 삭제하지 않는다.

권장 수정:

```sql
DELETE FROM fact_dimension_resolution_option
WHERE product_id = :product_id
  AND :is_locked = 0;
```

삭제 전 해당 14행은 별도 이력 테이블에 백업하는 것이 안전하다.

#### P1. 단일 실행기와 설정 파일

현재 실행명, DB 경로, OCR shard 수, targeted run 목록이 여러 파일에
하드코딩되어 있다.

권장 구조:

```text
config/
  pipeline.yaml
scripts/
  run_pipeline.py
```

`pipeline.yaml`에는 DB 경로, run version, worker 수, OCR 엔진, 카테고리
입력 경로, 출력 파일명을 둔다.

#### P1. 설치·실행 환경 명세

현재 확인된 실행 환경:

- Windows 11
- PowerShell 5.1
- Python 3.11.15
- Pillow 12.1.0
- requests 2.34.2
- urllib3 2.7.0
- Windows Runtime OCR, 한국어 언어팩

`requirements.txt`와 Windows OCR 사전점검 스크립트를 추가해야 한다.
PaddleOCR/Tesseract 비교 코드는 연구용이며 현재 최종 배치의 주 OCR은
Windows OCR이다.

#### P1. 카테고리 원본 입력 복원

운영 수집기는 다음 캐시를 입력으로 사용한다.

```text
poc_full_run/source_scope_cache.json
```

이 캐시는 원래 `제품군.xlsx`에서 비음영 카테고리를 읽어 만든 것이다.
현재 작업 폴더에는 원본 `제품군.xlsx`가 없고 `.pia` 파일만 있으므로,
카테고리를 새로 선정하려면 원본 Excel 또는 별도 CSV/DB 입력이 필요하다.

#### P1. 수집 범위 삭제 동기화

`collect_inventory()`는 신규·변경 상품을 upsert하지만, 새 scope에서
사라진 기존 상품을 자동 삭제하지 않는다. 카테고리 범위가 바뀌면
`active_scope` 또는 snapshot 기반으로 상품 유효기간을 관리해야 한다.

#### P2. 운영 안정성

- API endpoint는 비공식 내부 Ajax 경로이므로 schema 변경 감시가 필요하다.
- SSL 인증서 검증이 비활성화되어 있다.
- Q&A 상세 본문은 상품별 공개 Q&A 중 최대 3개만 추가 조회한다.
- 표준 logging/metric/retry dashboard가 없고 콘솔 출력 중심이다.
- 통합 테스트는 없고 규격 파서 중심 단위 테스트만 있다.
- hardcoded run name을 같은 DB에서 재실행하면 동일 실행명의 staging을
  덮어쓸 수 있으므로 실행 전 DB와 OCR 폴더 백업이 필요하다.

## 2. 시스템 범위

이 시스템은 LLM을 사용하지 않는다. API, HTML, FAQ/Q&A, 이미지 OCR 결과에
정규식, 카테고리 규칙, 모델명·옵션 매칭, 제외 규칙을 적용하는 결정론적
배치다.

```text
카테고리 scope
  ↓
상품 목록 API
  ↓
상품 API ─ 공간 API ─ 세트 API ─ Q&A API ─ PDP HTML
  ↓
products / sources
  ↓
기본 필드 파싱 + 상세 이미지 목록화
  ↓
1차 전체 이미지 OCR
  ↓
2차 layout OCR + 좌표·숫자 관측
  ↓
INFO/SIZE/DIMENSION 영역 고해상도 재OCR
  ↓
문맥 정규화·오탐 제외·옵션 선택
  ↓
단일 규격 원장
  ├─ 확정·잠금
  ├─ 비교정보 제공
  └─ 후보 없음
  ↓
필수값 PASS 판정
  ↓
요청 1·2 Excel 생성 및 DB 대조 검증
```

## 3. 입력 원천

### 3.1 카테고리 scope

`bulk_homestyle_collect.load_home_categories()`

- 입력: `poc_full_run/source_scope_cache.json`
- 조건: `source_system == "HOMESTYLE"`
- 현재 DB 카테고리: 60개
- 결과: `categories`, `products`

### 3.2 API와 HTML

`bulk_homestyle_collect.py`

| 원천 | endpoint/URL | 주요 산출 |
|---|---|---|
| 상품 목록 | `/displaysvc/ajax/v1/shop/goods` | 상품 ID, 카테고리 소속 |
| 상품 상세 | `/itemsvc/ajax/v1/pdp/goods/{productId}` | 상품명, 브랜드, 이미지, 옵션, 고시정보 |
| 공간 추천 | `/displaysvc/ajax/v1/collection/pdp/space-recommendation` | 공간·스타일·연관 제품 |
| 세트 구성 | `/itemsvc/ajax/v1/goods/packages` | 패키지·구성 상품 ID |
| Q&A 목록/상세 | `/itemsvc/ajax/v1/pdp/qna-list` | 공개 질문·답변 |
| PDP HTML | `https://homestyle.lge.co.kr/item?productId=...` | JSON-LD FAQ, visible dimension signal |

원문 JSON은 zlib 압축 BLOB으로 `sources`에 저장한다.

```text
products.listing_blob
sources.goods_blob
sources.space_blob
sources.packages_blob
sources.qna_blob
sources.html_blob
sources.ocr_blob
```

### 3.3 이미지

- 대표·롤링 이미지: 상품 API의 `data.images`
- 상세 이미지: 상품 API `detailInfo` HTML의 `img src/data-src`
- URL 정규화: 상대경로 절대화, 공백·한글 path/query 인코딩
- OCR 파일: `homestyle_bulk_run/ocr/{run_name}/`

## 4. 코드 구성

### 4.1 수집 계층

| 파일 | 역할 |
|---|---|
| `build_bulk_check_template.py` | 제품군 Excel을 scope cache로 변환하는 PoC |
| `bulk_homestyle_collect.py` | 카테고리·상품·API·Q&A·HTML 수집 |
| `bulk_homestyle_ocr.py` | 기본 상세 이미지 선별 OCR 및 `sources.ocr_blob` 병합 |
| `parse_faq_qna.py` | FAQ/Q&A 원천 연구·파싱 |

### 4.2 OCR 계층

| 파일 | 역할 |
|---|---|
| `run_dimension_scan_pass1.py` | 상세 이미지 전체를 빠르게 OCR하여 규격 후보 이미지를 선별 |
| `run_dimension_scan_pass2.py` | 선별 이미지 고해상도 layout OCR·좌표 관측 |
| `dimension_pass2_parser.py` | 좌표·축·숫자 묶음 파싱 |
| `run_dimension_targeted_reocr.py` | 사용자 피드백 회귀상품의 INFO/SIZE 영역 재OCR |
| `run_dimension_targeted_reocr_remaining.py` | 잔여 상품의 제목 영역 고해상도 재OCR |
| `run_dimension_partial_fusion.py` | W/H와 D/H 등 부분 축을 비교 후보로 결합 |
| `run_dimension_blocked_candidate_exposure.py` | 완성값이 있으나 자동 선택이 막힌 후보를 비교용으로 노출 |
| `run_dimension_pass2_raw_exposure.py` | 축 의미가 미확정인 pass2 숫자 묶음을 비교용으로 노출 |
| `run_windows_ocr*.ps1` | Windows OCR 실행 및 layout 좌표 JSON 생성 |

### 4.3 규칙·원장 계층

| 파일 | 역할 |
|---|---|
| `dimension_context_normalizer.py` | W/D/H 추출, 단위, 문맥 역할, 점수, OCR 보정 |
| `build_dimension_context_normalization.py` | 모든 원천 후보를 합치고 대표·옵션을 선택 |
| `refresh_dimension_context_selection.py` | targeted 후보를 현재 context snapshot에 재병합 |
| `build_dimension_resolution_ledger.py` | 최종 상태·잠금값·비교 후보·이력 원장 생성 |
| `build_dimension_remaining_reason_staging.py` | 최종 무후보 원인 분류 |
| `test_dimension_context_normalizer.py` | 21개 파서 회귀 테스트 |

### 4.4 출력 계층

| 파일 | 역할 |
|---|---|
| `build_product_component_staging.py` | 조합상품 판정과 `(product_id, component_seq)` 구성품 규격 snapshot 적재 |
| `test_product_component_staging.py` | 조합상품·구성품 반복 규격 파서 회귀 테스트 |
| `build_mandatory_pass_staging.py` | 별첨 0715 필수값 PASS/보강대상 DB 적재 |
| `build_homestyle_bulk_workbook.py` | 요청 1·2 전체 Excel 8개 시트 생성 |
| `validate_dimension_comparison_workbook.py` | Excel 상품·구성품 행, 순번, 상태, 색상을 DB와 대조 |
| `build_feasibility_sheet.py` | 외부 라이브러리 없이 XLSX ZIP/XML 작성 |

### 4.5 연구·과거 단계 코드

`research_*`, `analyze_*`, `build_ocr_benchmark.py`,
`collect_full_poc_sources.py`는 원인 연구·샘플·벤치마크 용도다.
`build_dimension_pattern_*`, `build_dimension_reinforcement_staging.py`,
`build_dimension_pass2_reclassification.py`는 최초 분류 마스터를 만들 때
사용한 bootstrap 분석 단계다. 운영 파이프라인으로 정리할 때는 이 로직을
하나의 versioned classification stage로 통합하는 것이 좋다.

## 5. 규격 처리 상태 전이

최종 authoritative 테이블:

```text
fact_dimension_resolution_ledger
```

| 상태 | 의미 | locked | needs_ocr |
|---|---|---:|---:|
| `SOURCE_CONFIRMED` | 기존 API/HTML/기본 파서에서 W/D/H 확정 | 1 | 0 |
| `RULE_RESOLVED` | OCR+문맥 규칙으로 대표 규격 확정 | 1 | 0 |
| `MANUAL_CONFIRMED` | 향후 수동 확정값 | 1 | 0 |
| `COMPARISON_PROVIDED` | 자동 확정하지 않고 후보를 사람에게 제공 | 0 | 0 |
| `NO_CANDIDATE` | 모든 처리 후 규격 후보 없음 | 0 | 1 |

상태 우선순위:

1. 기존 확정값은 항상 최우선이다.
2. 한 번 잠긴 규칙값은 명시적인 invalidation 규칙이 없으면 유지한다.
3. 비교정보 제공 상태는 이후 진단 snapshot이 없어져도 유지한다.
4. 배송·구성품·다른 라인업·허용오차 후보는 대표 규격에서 제외한다.
5. raw/pass2 후보는 비교정보로만 제공하고 자동 잠금하지 않는다.

주요 조회 뷰:

```sql
SELECT * FROM vw_dimension_progress_authoritative;
SELECT * FROM vw_dimension_locked_current;
SELECT * FROM vw_dimension_comparison_candidates_current;
SELECT * FROM vw_dimension_remaining_reason_current;
SELECT * FROM vw_dimension_remaining_reason_summary;
```

## 6. 규격 정규화 규칙

### 자동 확정 가능 규칙

- 명시적 `W/D/H`
- 가로/세로/높이, 폭/깊이/높이
- 가구 문맥의 순서형 3개 값
- `L/D/H → W/D/H`
- `W/L/H → W/D/H`
- `L/W/H → W/D/H`
- 원형의 지름+높이
- 액자·포스터 등 2D의 `W/H`, `D=N/A`
- 상품명 숫자·모델명·옵션코드 일치
- M/L, SS/Q/K 등 복수 옵션 분리

### 자동 확정 금지

- 배송·반입·엘리베이터·사다리차 수치
- 가드·발통·내부 선반 등 구성품 수치
- 다른 라인업 모델 수치
- 허용오차 `±3cm`
- 축 범위가 모호한 값
- pass2 원시 숫자 순서 또는 단위 추론값

### OCR 보정 예

```text
폭 1 500       → 폭1500
세로기054      → 세로2054
가로기600      → 가로2600
해드 높011,100 → 헤드높이1,100
```

보정은 특정 축 라벨과 숫자 패턴이 함께 있을 때만 수행한다.

## 7. 2단계 OCR 동작

### 1차 스캔

`run_dimension_scan_pass1.py`

1. 미확보 상품의 상세 이미지 URL을 전체 목록화한다.
2. 저비용 OCR로 각 이미지에서 SIZE, 규격, W/D/H, 단위, 숫자 묶음을 찾는다.
3. 배송·공통 배너 URL과 문맥에는 감점을 준다.
4. 후보 이미지만 pass2 대상으로 보낸다.

현재 실행 결과:

- 대상 상품 2,836개
- 상품-이미지 행 26,049개

### 2차 layout OCR

`run_dimension_scan_pass2.py`

1. 후보 이미지를 최대 폭 2,200px로 준비한다.
2. Windows layout OCR로 단어와 좌표를 얻는다.
3. 축 라벨, 숫자, 단위를 공간적으로 묶는다.
4. `stg_dimension_scan_pass2_observation`에 원시 관측을 저장한다.

현재 실행 결과:

- 대상 상품 2,670개
- 원시 관측 10,678개

### 제목 영역 재OCR

`run_dimension_targeted_reocr_remaining.py`

1. pass2 좌표에서 `INFO`, `DETAIL`, `한 눈에 보기`, `SIZE`,
   `DIMENSION`, `제품 사이즈`, `규격`을 찾는다.
2. 제목 위 300px부터 아래 1,900px를 자른다.
3. 폭 2,200px 확대, grayscale, autocontrast, contrast 1.35,
   sharpness 1.8을 적용한다.
4. 잘라낸 영역만 다시 OCR한다.

현재 결과:

- 영역 2,659개
- OCR 성공 2,659개

## 8. 비교정보 제공 정책

비교정보는 `fact_dimension_comparison_candidate`에 저장한다.

유형:

- 명확한 다중 옵션
- 여러 완성 후보가 충돌하는 경우
- 일부 축만 있는 경우
- 부분 후보를 결합한 경우
- 자동 선택을 막은 완성 후보
- pass2의 축 미확정 원시 숫자

원시 숫자 순서와 단위 추론은 다음 mapping으로 표시한다.

```text
PARTIAL_FUSION->W,D,H
BLOCKED_COMPLETE->COMPARISON
PASS2_RAW_VALUES->COMPARISON
```

`INFERRED_CM_FOR_COMPARISON`, `INFERRED_MM_FOR_COMPARISON`은 확정 단위가
아니므로 자동 PASS 규격으로 사용하면 안 된다.

## 9. DB 핵심 테이블

| 테이블 | 현재 행 | 역할 |
|---|---:|---|
| `categories` | 60 | 수집 카테고리 |
| `products` | 9,358 | 상품 inventory |
| `sources` | 9,358 | API/HTML/Q&A/OCR 원문 |
| `stg_dimension_scan_pass1_product_image` | 26,049 | 이미지별 1차 OCR 분류 |
| `stg_dimension_scan_pass2_observation` | 10,678 | 좌표 기반 원시 규격 관측 |
| `stg_dimension_targeted_ocr_crop` | 5,642 | 제목 영역 재OCR 결과 |
| `stg_dimension_targeted_ocr_candidate` | 16,664 | targeted·비교 후보 |
| `fact_dimension_resolution_ledger` | 9,358 | 상품별 최종 규격 상태 |
| `fact_dimension_resolution_option` | 5,776 | 잠금 옵션, 정리 대상 14행 포함 |
| `fact_dimension_comparison_candidate` | 17,580 | 사람 비교 후보 |
| `stg_dimension_remaining_reason` | 234 | 최종 무후보 원인 |
| `stg_product_combination` | snapshot 누적 | 전체 상품의 조합 판정·구성품 집계 |
| `stg_product_component_dimension` | snapshot 누적 | 조합상품 구성품별 W/D/H·근거 |
| `stg_mandatory_pass` | snapshot 누적 | 별첨 필수값 판정 이력 |
| `hist_dimension_resolution_event` | 13,639 | 원장 상태 변경 이력 |

## 10. 현재 DB에서 안전하게 다시 산출하는 순서

다음 순서는 네트워크 수집과 OCR을 다시 하지 않고 현재 DB의 후보를
재평가하여 Excel을 만드는 순서다.

```powershell
$env:PYTHONIOENCODING='utf-8'

python test_dimension_context_normalizer.py
python run_dimension_partial_fusion.py
python run_dimension_blocked_candidate_exposure.py
python run_dimension_pass2_raw_exposure.py
python refresh_dimension_context_selection.py
python build_dimension_resolution_ledger.py
python build_dimension_resolution_ledger.py
python build_dimension_remaining_reason_staging.py
python test_product_component_staging.py
python build_product_component_staging.py
python build_mandatory_pass_staging.py
python build_homestyle_bulk_workbook.py
python validate_dimension_comparison_workbook.py
```

두 번째 원장 실행의 `transitions_written`은 0이어야 한다.
`build_product_component_staging.py`는 반드시 Excel 생성 전에 실행해야 한다.
Excel 생성기는 current 조합 snapshot이 9,358개 상품과 일치하지 않으면
중단한다.

### 전체 수집 명령

```powershell
python bulk_homestyle_collect.py all --workers 16 --retry-errors
```

주의:

- 현재 scope에서 사라진 과거 상품은 자동 삭제되지 않는다.
- 내부 API와 이미지 원천을 다시 호출한다.
- 실행 전 SQLite와 OCR 폴더를 백업한다.

### OCR 명령

```powershell
python run_dimension_scan_pass1.py all --workers 8 --ocr-workers 4 --shards 8
python run_dimension_scan_pass2.py all --workers 8 --ocr-workers 4 --shards 8
python run_dimension_targeted_reocr.py all --workers 8
python run_dimension_targeted_reocr_remaining.py all --workers 8 --ocr-workers 8 --shards 8
```

현재 run name이 코드에 고정되어 있으므로, 기존 실행을 보존하려면 코드를
versioned run name으로 변경하거나 DB/OCR 폴더를 복제한 후 실행해야 한다.

## 11. 입력→산출 예

### API에서 바로 확정

상품 `G25070005743`

입력:

```text
상품정보고시 크기:
[4인] W2910 X D1020 X H910mm /
[스툴] W740 X D660 X H410mm
```

산출:

```text
resolution_status = SOURCE_CONFIRMED
대표 W/D/H = 2910 / 1020 / 910
```

상품명·대표 제품 문맥 때문에 4인 소파를 대표값으로 사용하고 스툴은
구성품 규격으로 남긴다.

### OCR 보정 후 규칙 확정

상품 `G25090019410`

입력 OCR:

```text
INFO ... 가로기600 * 세로2140 * 헤드높이800mm
```

정규화:

```text
가로기600 → 가로2600
```

산출:

```text
resolution_status = RULE_RESOLVED
W/D/H = 2600 / 2140 / 800
resolution_rule_code = EXPLICIT_WDH
```

### 부분값을 비교정보로 제공

상품 `G25110025989`

입력 OCR:

```text
W590 H810
D570 H450
```

산출 후보:

```text
590 / 570 / 450
590 / 570 / 810
```

```text
resolution_status = COMPARISON_PROVIDED
```

좌면 높이와 전체 높이를 자동으로 단정하지 않고 둘 다 제공한다.

### 최종 무후보

상품 `G25070000483`

입력:

```text
사이즈 옵션: Ø100, Ø120
높이 정보 없음
이미지 OCR에서도 추가 규격 신호 없음
```

산출:

```text
resolution_status = NO_CANDIDATE
reason_code = PASS1_NO_SIZE_SIGNAL
```

## 12. 현재 검증 수치

| 상태 | 상품 수 |
|---|---:|
| `SOURCE_CONFIRMED` | 4,152 |
| `RULE_RESOLVED` | 1,561 |
| `COMPARISON_PROVIDED` | 3,411 |
| `NO_CANDIDATE` | 234 |
| 합계 | 9,358 |

기타 검증:

- 확정·잠금: 5,713
- 비교 후보: 17,580
- 확정 또는 비교 가능: 9,124, 97.5%
- 필수값 전체 PASS: 7,372, 78.8%
- SQLite integrity: `ok`
- foreign key 오류: 0
- 상품 원장 누락·중복: 0
- Excel 비교 후보 정렬 오류: 0

## 13. 제품화 완료 기준

다음 조건을 충족하면 PoC가 아닌 운영 배치로 볼 수 있다.

1. 빈 폴더에서 scope 입력 하나로 DB와 Excel을 재구축한다.
2. 실행별 run ID가 외부 설정으로 관리된다.
3. 단계별 실패 후 resume가 가능하다.
4. schema migration과 requirements가 version control에 포함된다.
5. 과거 옵션 14행이 정리되고 자동 검증된다.
6. API schema fixture, OCR fixture, DB integration test가 있다.
7. 수집 범위에서 제외된 상품의 비활성화 정책이 있다.
8. 필드별 source/evidence/confidence를 평면 결과 테이블 또는 API로 제공한다.
9. 운영 로그, 처리량, 오류율, OCR 비용·시간을 확인할 수 있다.
