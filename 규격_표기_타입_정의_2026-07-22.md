# 규격 표기 타입 정의 (2026-07-22)

## 컬럼 역할 분리

`요청1_대표 규격 대상`은 규격이 가리키는 대상을 나타낸다.

- `제품 외형`: 대표 제품의 전체 외형
- `제품/옵션`: 색상·사이즈 등 옵션별 외형
- `제품/구성품`: 본체·스툴·프레임 등 구성품별 외형

이 값은 `W/D/H`, `L/D/H` 같은 원문 표기 타입이 아니므로 그대로 유지한다.

별도 추가 필드:

| 필드 | 의미 |
|---|---|
| `규격 표기 타입 코드` | 원문의 축·숫자 표기 패턴 |
| `원문 축 순서` | 예: `L>W>H`, `H>W>D` |
| `W/D/H 매핑 규칙` | 원문 축을 표준 W/D/H로 바꾸는 규칙 또는 후보 |
| `축 매핑 상태` | 자동 매핑, 의미 확인, 정책 확인, 누락 축, 신호 없음 |
| `패턴 근거` | 타입 판정에 사용한 원문/옵션/OCR 근거 |

향후 내부 작업용 시트에서는 다음 컬럼명으로 연결한다.

- `요청1_규격 표기 타입`
- `요청1_원문 축 순서`
- `요청1_W/D/H 매핑 규칙`
- `요청1_규격 타입 판정상태`

## 주요 타입

| 타입 코드 | 원문 예 | W/D/H 처리 | 기본 상태 |
|---|---|---|---|
| `W_D_H_EXPLICIT` | `W 800 × D 600 × H 750` | W→W, D→D, H→H | `MAPPED` |
| `KOREAN_W_D_H` | `가로 800, 깊이 600, 높이 750` | 가로→W, 깊이→D, 높이→H | `MAPPED` |
| `L_D_H_EXPLICIT` | `L 800 × D 600 × H 750` | L→W 후보, 의미 확인 후 적용 | `INTERPRETATION_REQUIRED` |
| `L_W_H_EXPLICIT` | `L 190 × W 80 × H 75` | L→W, 원문 W→D 후보 | `INTERPRETATION_REQUIRED` |
| `W_L_H_EXPLICIT` | `W 80 × L 190 × H 75` | W→W, L→D 후보 | `INTERPRETATION_REQUIRED` |
| `DIAMETER_H` | `Ø 400 × H 600` | 원형이면 직경→W·D, H→H | `CATEGORY_POLICY_REQUIRED` |
| `UNLABELED_3_AXIS` | `800 × 600 × 750` | 순서 기반 W/D/H 후보 | `ORDER_REVIEW_REQUIRED` |
| `W_D_PAIR` | `W 1600 × D 2300` | H 추가 추출 또는 비적용 정책 | `INCOMPLETE_OR_NA_POLICY` |
| `W_H_PAIR` | `W 600 × H 900` | D 추가 추출 또는 비적용 정책 | `INCOMPLETE_OR_NA_POLICY` |
| `L_W_PAIR` | `L 200 × W 150` | L/W 의미와 누락 축 확인 | `INTERPRETATION_REQUIRED` |
| `UNLABELED_2_AXIS` | `160 × 230 cm` | 카테고리별 W/D 또는 W/H | `CATEGORY_POLICY_REQUIRED` |
| `PARTIAL_W_D` | 현재 W·D만 확보 | H 추가 추출 또는 비적용 정책 | `MISSING_AXIS` |
| `PARTIAL_W_H` | 현재 W·H만 확보 | D 추가 추출 또는 비적용 정책 | `MISSING_AXIS` |
| `PARTIAL_D_H` | 현재 D·H만 확보 | W 추가 추출 | `MISSING_AXIS` |
| `STANDARD_SIZE_CODE` | `SS`, `Q`, `K` | 브랜드·모델 규격표 연결 | `SOURCE_TABLE_REQUIRED` |
| `NO_CONFIRMED_PATTERN` | 숫자·축 신호 없음 | 상세 이미지 OCR/원천 보강 | `NO_SIGNAL` |

전체 타입 기준은 DB의 `ref_dimension_notation_type`을 원본으로 사용한다.

## 현재 규격 보강대상 분류

규격 보강대상 5,208개 기준:

| 표기 타입 | 건수 | 의미 |
|---|---:|---|
| `NO_CONFIRMED_PATTERN` | 4,434 | 현재 확정 패턴 없음 |
| `PARTIAL_W_D` | 422 | W/D 확보, H 누락 |
| `W_D_PAIR` | 119 | W/D 2축 또는 러그 축 정책 대상 |
| `W_L_H_EXPLICIT` | 113 | 원문 순서 W/L/H, L 의미 확인 필요 |
| `W_H_PAIR` | 78 | W/H 2축 또는 액자·포스터 축 정책 대상 |
| `PARTIAL_W_H` | 18 | W/H 확보, D 누락 |
| `L_W_H_EXPLICIT` | 10 | 원문 순서 L/W/H, L 의미 확인 필요 |
| `DIAMETER_H` | 6 | 직경의 W/D 복제 정책 필요 |
| `L_D_H_EXPLICIT` | 4 | L/D/H 의미 확인 필요 |
| `PARTIAL_D_H` | 4 | D/H 확보, W 누락 |

대표 규격 대상 타입:

| 대상 타입 | 건수 |
|---|---:|
| `PRODUCT_EXTERIOR` | 4,611 |
| `PRODUCT_OPTION` | 552 |
| `PRODUCT_COMPONENT` | 45 |

## 적용 원칙

1. `MAPPED` 타입만 규칙 기반 자동 변환을 허용한다.
2. L 포함 타입은 원문의 L을 무조건 W로 치환하지 않는다.
3. 평면형·원형 상품은 카테고리별 비적용 축 정책을 먼저 확정한다.
4. 원문 축 순서와 근거를 보존하고, 숫자 W/D/H 컬럼에는 확정값만 넣는다.
5. 한 상품에 여러 규격 패턴이 있으면 대표 타입과 전체 감지 타입을 함께 저장한다.

## DB 객체

- 타입 기준표: `ref_dimension_notation_type`
- 상품별 현재 타입: `stg_dimension_pattern`
- 타입별 요약: `vw_dimension_pattern_current_summary`
- 규격 보강대상+타입 결합: `vw_dimension_reinforcement_with_pattern`
- 갱신 프로그램: `build_dimension_pattern_staging.py`
- 최신 실행 결과: `homestyle_bulk_run/dimension_pattern_staging_latest.json`

현재 스냅샷은 규격 보강대상 5,208개를 대상으로 하며 Excel은 생성하지 않았다.
