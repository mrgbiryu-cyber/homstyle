# 1차 OCR 파싱 대상 이미지 분류 결과

## 실행 범위

- 실행명: `dimension_scan_pass1_all_detail_images_v1`
- 대상: `stg_dimension_ocr_review_wide.is_current=1`이며 `has_human_review_information=0`인 2,455개 상품
- 범위: 각 상품 상세 HTML에 포함된 전체 정지 이미지
- 목적: 규격값을 확정하지 않고 2차 정밀 OCR에 넘길 이미지만 선별
- 원본 규격값 및 기존 `sources` 데이터 반영: 하지 않음
- Excel 생성·수정: 하지 않음

## 실행 결과

| 항목 | 결과 |
|---|---:|
| 대상 상품 | 2,455개 |
| 상품-이미지 연결행 | 20,208건 |
| 고유 이미지 URL | 6,225장 |
| OCR 성공 | 6,201장 (99.6%) |
| 기존 OCR 재사용 | 1,786장 |
| 신규 OCR 성공 | 4,415장 |
| 다운로드 오류 | 24장 |
| 2차 후보 고유 이미지 | 1,856장 |
| 2차 후보 상품-이미지 연결행 | 4,292건 |
| 2차 OCR 입력 감소율 | 78.8% |
| 후보 이미지가 있는 상품 | 2,308개 |
| 전체 이미지 무신호 상품 | 136개 |
| 스캔 미완료 상품 | 2개 |
| 상세 이미지 없음 | 9개 |

동일 이미지 URL이 여러 상품에 반복되는 경우 OCR은 한 번만 수행하고, 결과를 각 상품의 이미지 순서에 다시 연결했다.

## 1차 후보 판정 신호

- `제품 사이즈`, `제품 크기`, `규격`, `치수`, `Size`, `Dimension`, `Measurement`
- 숫자와 함께 인식된 `W`, `D`, `H`, `L`, 가로, 세로, 폭, 너비, 깊이, 높이, 길이, 지름
- `mm`, `cm`, `㎜`, `㎝`
- `숫자 × 숫자`, `숫자 × 숫자 × 숫자` 패턴
- 사이즈 문구 주변의 단위 없는 숫자 군집
- URL의 `size`, `dimension`, `measure`, `drawing`, `spec` 힌트

배송·반품 공통 이미지의 약한 문구가 후보를 부풀리지 않도록 `notice`, `footer`, `policy`, `delivery`, `shipping`, `customer`, `return`, `refund`, `faq` URL은 명확한 다축·다값 증거가 없을 때 `GENERIC_NOTICE_IMAGE_SUPPRESSED`로 분리했다.

## 후보 분류

| 분류 | 고유 이미지 수 | 2차 대상 |
|---|---:|:---:|
| `SIZE_LABEL_AXIS_UNIT` | 448 | O |
| `SIZE_LABEL_WITH_UNIT` | 626 | O |
| `SIZE_LABEL_WITH_AXIS` | 286 | O |
| `SIZE_LABEL_UNIT_MISSING` | 151 | O |
| `UNIT_NUMERIC_CLUSTER` | 224 | O |
| `PARTIAL_AXIS_FOUND` | 74 | O |
| `DIMENSION_PAIR_FOUND` | 24 | O |
| `DIMENSION_TRIPLE_FOUND` | 8 | O |
| `MULTI_AXIS_FOUND` | 11 | O |
| `URL_HINT_ONLY` | 4 | O |
| `WEAK_SIZE_WORD_ONLY` | 162 | X |
| `GENERIC_NOTICE_IMAGE_SUPPRESSED` | 33 | X |
| `NO_SIZE_SIGNAL` | 4,150 | X |
| `SCAN_OCR_ERROR` | 24 | X/재시도 |

`SIZE_LABEL_UNIT_MISSING`은 단위나 W/D/H 축이 없어도 `LAMP SIZE 45.5 × 151`처럼 사람이 확인하거나 2차 OCR로 복원할 수 있는 이미지를 보존하기 위한 분류다.

## DB 산출물

- `stg_dimension_scan_pass1_unique_image`: 고유 URL별 OCR 원문, 후보 여부, 점수, 분류, 사유 코드, 신호 발췌문
- `stg_dimension_scan_pass1_product_image`: 상품 ID, 상세 이미지 순서, URL, 후보 여부 연결행
- `stg_dimension_scan_pass1_product`: 상품별 전체 이미지 수, OCR 성공·오류 수, 후보 이미지 수와 URL 목록, 상품 판정
- `vw_dimension_scan_pass1_candidate_images`: 2차 OCR 후보 이미지만 조회하는 전용 뷰

후보 조회 예시:

```sql
SELECT product_id, image_order, image_url, classification,
       candidate_score, reason_codes, signal_excerpt
FROM vw_dimension_scan_pass1_candidate_images
WHERE run_name = 'dimension_scan_pass1_all_detail_images_v1'
ORDER BY product_id, image_order;
```

## 미완료 예외

- `G25100019860`: 상세 이미지 5장 중 4개 URL이 HTTP 404
- `G26050041005`: 상세 이미지 5장 중 1개 URL이 HTTP 404

그 외 다운로드 오류 URL은 해당 상품에 다른 OCR 성공 이미지 또는 후보 이미지가 있어 상품 전체를 `SCAN_INCOMPLETE`로 분류하지 않았다. 상세 이미지 자체가 없는 상품은 9개다.

## 다음 단계 경계

이번 실행은 1차 스캐닝까지만 완료했다. 후보 4,292개 상품-이미지 연결행에 대한 영역 탐지, 고해상도 재OCR, W/D/H/L 의미 매핑 및 규격값 확정은 수행하지 않았다.
