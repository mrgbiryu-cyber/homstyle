# 전체 9,358개 규격 분류 마스터 (2026-07-23)

## 1. 공식 모수와 완료 기준

전체 홈스타일 상품 9,358개를 한 개의 마스터 테이블로 적재했다.

`분류 완료`는 W/D/H 값이 모두 확정됐다는 의미가 아니라, 현재까지의 API/HTML/OCR 및 2차 좌표 OCR 결과를 바탕으로 상품에 다음 처리 패턴 또는 완료 상태가 부여됐다는 의미이다.

| 분류 상태 | 상품 수 | 비율 |
|---|---:|---:|
| 분류 완료 | 9,151 | 97.8% |
| 미분류 잔여 | 207 | 2.2% |
| **전체** | **9,358** | **100.0%** |

## 2. 분류 완료 9,151개 구성

| 완료 단계 | 상품 수 | 설명 |
|---|---:|---|
| `BASELINE_WDH_CONFIRMED` | 4,152 | 기존 W/D/H 값이 모두 확정된 상품 |
| `PRE_PASS2_PATTERN_CLASSIFIED` | 2,751 | 기존 구조값·OCR 결과로 세부 패턴이 부여된 상품 |
| `PASS2_OCR_CLASSIFIED` | 2,248 | 2차 좌표 OCR에서 규격 관측 형태가 분류된 상품 |
| **합계** | **9,151** |  |

규격값 확정과 분류 완료는 별도 컬럼으로 관리한다.

- `classification_complete=1`: 현재 처리 패턴까지 분류 완료
- `dimension_value_confirmed=1`: W/D/H 값까지 확정 완료
- W/D/H 값 확정 완료: 4,152개
- 2차 OCR 명시적 W/D/H 후보: 736개
- 나머지 분류 완료 상품은 단위·축·순서·부분값 등의 후속 검토 상태를 유지한다.

## 3. 미분류 잔여 207개

| 잔여 코드 | 상품 수 | 원인 |
|---|---:|---|
| `N01_PASS2_IMAGE_FETCH_RETRY` | 46 | 2차 OCR 대상 이미지 재수집 실패 |
| `N02_PASS2_NO_PATTERN` | 14 | 2차 OCR은 성공했지만 규격 관측 패턴 없음 |
| `N03_PASS1_SOURCE_RETRY` | 2 | 1차 이미지 소스 오류 |
| `N04_NO_DETAIL_IMAGE` | 9 | 상세 이미지 없음 |
| `N05_PASS1_NO_SIZE_SIGNAL` | 136 | 전체 상세 이미지에서 규격 신호 없음 |
| **합계** | **207** |  |

## 4. DB 객체

### 마스터 테이블

- `stg_dimension_classification_master`: 전체 9,358개 상품의 현재 분류 완료 여부, 완료 단계, 규격값 확정 여부, 패턴 코드, 2차 OCR 상태, 후속 작업

### 조회 뷰

- `vw_dimension_classification_master_current`: 전체 9,358개
- `vw_dimension_classification_complete_current`: 분류 완료 9,151개
- `vw_dimension_classification_remaining_current`: 미분류 잔여 207개
- `vw_dimension_classification_summary`: 완료/잔여 단계별 집계
- `vw_dimension_resolution_summary`: 규격값 확정·검토 상태별 집계

실행 요약은 `homestyle_bulk_run/dimension_classification_master_latest.json`에 저장했다.

## 5. 이전 2,455개 통계와의 관계

`stg_dimension_pass2_reclassification`의 2,455개는 전체 상품이 아니라 1차 OCR 당시 규격 무정보군이다.

해당 부분집합의 재처리 큐 576개는 다음 두 종류를 합한 수치였다.

- 숫자 관측값은 있으나 의미를 다시 나눈 상품: 369개
- 규격 관측값이 없는 상품: 207개

369개는 현재 V01~V07 패턴으로 분류됐으므로 전체 마스터에서는 분류 완료에 포함한다. 따라서 전체 9,358개 기준 실제 미분류 잔여는 207개이다.

## 6. 반영 경계

- 전체 상품 분류 마스터와 조회 뷰만 추가했다.
- 기존 상품 원천 W/D/H 값은 변경하지 않았다.
- Excel 파일은 변경하지 않았다.
