# 조합상품 식별 및 구성품별 규격 다중 행 구현 결과

> **구현 당시 snapshot 문서**  
> 아래 `검토필요 270상품·구성품 2,111행`은 침대 단일 Asset 정책 적용
> 전 수치다. 현재는 침대 121개가 완료 처리되어 `체크필요 149상품·구성품
> 1,990행`이다. 최종 패턴 코드와 상품별 매핑은
> [`조합상품_3D변환_패턴목록_2026-07-27.md`](./조합상품_3D변환_패턴목록_2026-07-27.md)를
> 기준으로 한다. 이 문서는 `product_id + component_seq` 구현 근거로만
> 유지한다.

## 결론

같은 `product_id`를 유지하면서 `component_seq`를 추가해 구성품마다 한 행을
저장하는 방식으로 구현했다.

기존 DB도 규격 후보와 옵션을 각각 `(product_id, candidate_no)` 및
`(product_id, option_no)` 복합키로 저장하므로, 동일한 구조를 구성품 규격에
적용할 수 있다.

적용한 키:

- 상품 원장: `product_id`
- 구성품 규격: `(product_id, component_seq)`

## 1차 식별 결과

전체 9,358개 상품명을 기준으로 다음 표기를 가진 상품을 조합상품 1차 후보로
탐색했다.

- `세트`
- `패키지` 또는 `package`
- `+` 또는 `＋`

결과:

- 1차 후보: 1,152개
- 명시적으로 `세트/패키지`인 상품: 752개
- `소파 + 스툴/오토만` 상품: 130개
- 두 집합을 합친 고신뢰 조합상품: 882개
- 그 밖의 `+` 표기 검토대상: 270개

`+`만으로는 다음과 같은 과탐이 발생한다.

- 모델명: `아카이브+`, `TERRACE+`, `Lotus+`
- 옵션 표현: `바퀴+글라이드`
- 작품명: `Reality + Image`
- 색상·소재 결합: `블러쉬+에그화이트`

따라서 최종 `is_combination_product`는 단순 기호 검출이 아니라 구성품명
사전과 제외 패턴을 함께 사용해야 한다.

## API 확인 결과

`packages API`는 9,358개 모두 HTTP 200이지만, 자신 이외의 실제 구성
`productId`가 포함된 상품은 현재 0개다.

따라서 아래 두 개념을 분리해야 한다.

- `component_product_id`: 실제 별도 상품 ID. 현재 packages API에서는 미확보.
- `component_seq`: 하나의 판매 product 안에 포함된 물리 구성품 순번.

## 대표 예제

상품:

- `G25070005743`
- `[by CASAMIA] 벨로씨 레보르 천연면피 가죽 소파 4인＋스툴`

상품 API 상품정보고시의 크기:

```text
[4인] W2910 X D1020 X H910mm / [스툴] W740 X D660 X H410mm
```

기존 상품 대표 규격은 첫 번째 규격을 대표값으로 유지한다.

```text
G25070005743 | 대표 | 2910 | 1020 | 910
```

별도 구성품 원장의 현재 결과:

```text
product_id    | component_seq | component_name | W(mm) | D(mm) | H(mm)
G25070005743  | 1             | 4인 소파       | 2910  | 1020  | 910
G25070005743  | 2             | 스툴            | 740   | 660   | 410
```

130개 `소파+스툴/오토만` 가운데 7개는 상품정보고시에 두 구성품의 규격과
라벨이 모두 명시되어 있어 바로 다중 행으로 전환했다. 전체 조합상품 유형을
포함하면 모든 구성품 규격이 API 명시값으로 확정된 상품은 39개다. 나머지는
후보·부분확보·미확보 상태를 유지하여 HTML·FAQ/Q&A·상세 이미지 OCR에서
구성품별 규격을 추가 탐색하도록 분리했다.

## 구현 DB 구조

`build_product_component_staging.py`가 매 실행마다 새 snapshot을 적재한다.

| 객체 | 역할 |
|---|---|
| `stg_product_combination` | 9,358개 전체 상품의 조합상품 판정 및 구성품 집계 |
| `stg_product_component_dimension` | `(snapshot_id, product_id, component_seq)`별 구성품 규격 |
| `vw_product_combination_current` | 상품별 현재 조합 판정 |
| `vw_product_component_dimensions_current` | 구성품별 현재 규격 행 |
| `vw_product_combination_summary` | 조합 판정·구성품 규격 상태 요약 |

상품 레벨 주요 컬럼:

- `is_combination_product`
- `combination_detection_status`
  - `CONFIRMED`
  - `CANDIDATE`
  - `NOT_COMBINATION`
- `component_count`
- `combination_rule_code`
- `component_confirmed_count`
- `component_output_status`

구성품 레벨 주요 컬럼:

- `product_id`
- `component_seq`
- `component_name`
- `component_type`
- `component_quantity`
- `is_primary`
- `w_mm`, `d_mm`, `h_mm`
- `resolution_status`
- `source_type`, `source_ref`, `evidence_text`

현재 적재 결과:

| 구분 | 상품/행 수 |
|---|---:|
| 조합상품 확정 | 882상품 |
| 조합상품 검토필요 | 270상품 |
| 비조합상품 | 8,206상품 |
| 구성품 행 | 2,111행 |
| 모든 구성품 규격 확정 | 39상품 |
| 구성품 규격 후보 | 84상품 |
| 구성품 규격 부분확보 | 17상품 |
| 구성품 규격 미확보 | 353상품 |
| 구성품명 추가 분리 필요 | 389상품 |

## Excel 출력 원칙

상품 기본정보 시트는 기존처럼 상품당 한 행을 유지한다.

`01_상품별_요구필드`에는 다음 5개 컬럼을 추가했다.

- `요청1_조합상품 여부`
- `요청1_조합상품 판정근거`
- `요청1_조합 구성품 수`
- `요청1_구성품 규격 확정 수`
- `요청1_구성품 분리 상태`

`07_조합상품_구성품` 시트에서는 동일 `product_id`를 반복하고
`component_seq`로 행을 분리한다. 이렇게 해야 상품 수 통계가 구성품 수만큼
늘어나는 문제를 막을 수 있다.

규격 후보·부분확보·미확보와 구성품명 추가 분리가 필요한 셀은 노란색 또는
분홍색으로 표시한다. 확정되지 않은 값을 임의로 채우지 않는다.

통계 기준:

- 상품 수: `COUNT(DISTINCT product_id)`
- 구성품 규격 수: `COUNT(*)`
- 조합상품 PASS: 필수 규격 대상 구성품이 모두 확정된 경우에만 PASS

## 실행 순서

```powershell
python build_product_component_staging.py
python build_mandatory_pass_staging.py
python build_homestyle_bulk_workbook.py
python validate_dimension_comparison_workbook.py
```

`build_product_component_staging.py`는 상품명 조합 규칙, 상품정보고시의
`[구성품명] W×D×H` 반복 블록, 명시 단위를 파싱한다. 단위가 없는 값은
`API_UNIT_INFERRED`, 구성품명이나 규격이 부족한 값은 검토 상태로 저장한다.

필수값 PASS 7,372개 통계는 별첨 0715의 기존 6개 필수 그룹 기준으로 유지한다.
다만 요청 1 전체 완료 통계에서는 조합상품 구성품이 모두 확정되지 않은 상품을
사람 검토 또는 보강 대상으로 계산한다.
