# 규격 분류 DB 및 미분류 재정렬 결과 (2026-07-23)

> 범위 정정: 이 문서의 2,455개는 전체 상품이 아니라 `1차 규격 무정보군` 부분집합이다. 전체 9,358개 기준 공식 통계는 `전체_9358_규격분류_마스터_2026-07-23.md`와 `stg_dimension_classification_master`를 사용한다. 부분집합의 재처리 큐 576개는 값 의미 재검토 369개를 포함하므로 전체 미분류 잔여 수가 아니다. 전체 기준 실제 미분류 잔여는 207개이다.

## 1. 분류 결과 DB 반영

1차 무정보군 2,455개 전체를 `stg_dimension_pass2_reclassification`에 적재했다.

| 상태 | 상품 수 | 설명 |
|---|---:|---|
| `DIRECT_WDH_CANDIDATE` | 736 | 명시적 W/D/H+단위 후보 |
| `PATTERN_CLASSIFIED_REVIEW` | 1,143 | 단위 누락, L/W/H, 축 미표기 3값, 일부 축 등의 패턴까지 분류됨 |
| `VALUE_RECLASSIFICATION_REQUIRED` | 369 | 숫자 값은 있으나 의미·순서를 다시 분류해야 함 |
| `NO_OBSERVATION_REQUEUE` | 207 | 규격 관측값 자체가 없음 |
| **합계** | **2,455** |  |

- OCR 관측 형태까지 분류된 상품: 2,248개
- 패턴까지 분류된 상품: 1,879개
- 다시 정렬한 재처리 큐: 576개
  - 값 의미 재분류: 369개
  - 무관측 재처리: 207개

## 2. 값 의미 미분류 369개 재정렬

| 우선순위 | 패턴 | 상품 수 | 다음 판단 |
|---:|---|---:|---|
| 1 | `V01_2D_PAIR_WITH_UNIT` | 68 | 액자·러그 등의 가로/세로와 두께 비적용 정책 검토 |
| 2 | `V02_3D_TRIPLE_WITH_UNIT` | 152 | 3개 값의 W/D/H 순서 규칙 생성 |
| 3 | `V03_2D_TRIPLE_WITH_UNIT` | 49 | 두께 포함 규격인지 옵션 크기 나열인지 구분 |
| 4 | `V04_3D_PAIR_WITH_UNIT` | 5 | 누락 축 또는 지름×높이 표기 확인 |
| 5 | `V05_3D_TRIPLE_UNIT_REQUIRED` | 71 | 3개 값의 단위부터 검증 |
| 6 | `V06_2D_TRIPLE_UNIT_REQUIRED` | 18 | 2D 제품의 두께/옵션과 단위 검증 |
| 7 | `V07_3D_PAIR_UNIT_REQUIRED` | 6 | 단위와 누락 축 보강 |
|  | **합계** | **369** |  |

값 품질 플래그는 다음과 같이 분리했다.

- 기본 범위가 타당한 후보: 259개
- 단위 확인 필요: 90개
- 물리 범위 이상 검토: 15개
- OCR 숫자 붙음 의심: 5개

예를 들어 `Cestita Bateria`의 `220 × 8.6 × 11066`은 `OCR_DIGIT_JOIN_REVIEW`로 분리했다. 이 값들은 패턴 적용 대상에 바로 섞이지 않는다.

## 3. 관측값 없는 207개 재정렬

| 우선순위 | 패턴 | 상품 수 | 다음 작업 |
|---:|---|---:|---|
| 20 | `N01_PASS2_IMAGE_FETCH_RETRY` | 46 | 실패 이미지 URL 재수집 후 좌표 OCR |
| 21 | `N02_PASS2_NO_PATTERN` | 14 | OCR 전체 원문·이미지 확인 후 탐지 어휘 확장 |
| 22 | `N03_PASS1_SOURCE_RETRY` | 2 | 상세 이미지 소스 재수집 후 1차 OCR부터 재실행 |
| 23 | `N04_NO_DETAIL_IMAGE` | 9 | HTML/API/외부 판매처에서 규격 정보 보강 |
| 24 | `N05_PASS1_NO_SIZE_SIGNAL` | 136 | HTML·FAQ/Q&A·옵션·외부 설명 순으로 다른 소스 조사 |
|  | **합계** | **207** |  |

## 4. DB 객체

### 테이블

- `stg_dimension_pass2_reclassification`: 2,455개 상품의 현재 분류, 원시값, 단위, 이미지 URL, OCR 근거, 재처리 우선순위

### 뷰

- `vw_dimension_pass2_observed_classified_current`: 관측 형태가 분류된 2,248개
- `vw_dimension_pass2_pattern_classified_current`: 패턴까지 분류된 1,879개
- `vw_dimension_pass2_value_reclassification_current`: 값 의미 재분류 369개
- `vw_dimension_pass2_unclassified_current`: 관측값 없는 207개
- `vw_dimension_pass2_requeue_current`: 우선순위로 정렬된 전체 재처리 큐 576개
- `vw_dimension_pass2_requeue_summary`: 패턴·단위·품질별 집계

실행 요약은 `homestyle_bulk_run/dimension_pass2_reclassification_latest.json`에 저장했다.

## 5. 반영 경계

- 이번 작업은 분류 스테이징 테이블과 조회 뷰를 추가한 것이다.
- 기존 상품 원천 W/D/H 값은 변경하지 않았다.
- Excel 파일은 변경하지 않았다.
- 분류표의 2D/3D는 재처리 순서를 위한 카테고리 프로필이며 최종 규격 적용 판정은 아니다.
